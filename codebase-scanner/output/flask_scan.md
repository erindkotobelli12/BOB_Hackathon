# 📚 Documentation Report: src

**Generated:** 2026-05-02 20:32:04 UTC
**Root Directory:** `C:\tmp\flask\src`

## 📊 Coverage Summary

| Language | Files | Functions | Coverage % |
|----------|-------|-----------|------------|
| python | 24 | 359 | 60.2% |
| **Total** | **24** | **359** | **60.2%** |

## ⚠️ Undocumented Items

Found **100** undocumented functions/methods:

### `C:\tmp\flask\src\flask\app.py`

- **_make_timedelta** (function) - Line 73
- **remove_ctx** (function) - Line 85
- **add_ctx** (function) - Line 97
- **Flask.__init_subclass__** (method) - Line 254
- **Flask.__init__** (method) - Line 310

### `C:\tmp\flask\src\flask\blueprints.py`

- **Blueprint.__init__** (method) - Line 19

### `C:\tmp\flask\src\flask\cli.py`

- **locate_app** (function) - Line 230
- **locate_app** (function) - Line 236
- **locate_app** (function) - Line 241
- **get_version** (function) - Line 267
- **ScriptInfo.__init__** (method) - Line 305
- **_set_app** (function) - Line 440
- **_set_debug** (function) - Line 468
- **_env_file_callback** (function) - Line 493
- **FlaskGroup.__init__** (method) - Line 563
- **FlaskGroup._load_plugin_commands** (method) - Line 600
- **FlaskGroup.get_command** (method) - Line 609
- **FlaskGroup.list_commands** (method) - Line 636
- **FlaskGroup.make_context** (method) - Line 657
- **FlaskGroup.parse_args** (method) - Line 678
- **CertParamType.__init__** (method) - Line 788
- **CertParamType.convert** (method) - Line 791
- **SeparatedPathType.convert** (method) - Line 873
- **main** (function) - Line 1122

### `C:\tmp\flask\src\flask\config.py`

- **ConfigAttribute.__init__** (method) - Line 23
- **ConfigAttribute.__get__** (method) - Line 30
- **ConfigAttribute.__get__** (method) - Line 33
- **ConfigAttribute.__get__** (method) - Line 35
- **ConfigAttribute.__set__** (method) - Line 46
- **Config.__init__** (method) - Line 94
- **Config.__repr__** (method) - Line 366

### `C:\tmp\flask\src\flask\ctx.py`

- **_AppCtxGlobals.__getattr__** (method) - Line 53
- **_AppCtxGlobals.__setattr__** (method) - Line 59
- **_AppCtxGlobals.__delattr__** (method) - Line 62
- **_AppCtxGlobals.__contains__** (method) - Line 105
- **_AppCtxGlobals.__iter__** (method) - Line 108
- **_AppCtxGlobals.__repr__** (method) - Line 111
- **AppContext.__init__** (method) - Line 300
- **AppContext.__enter__** (method) - Line 506
- **AppContext.__exit__** (method) - Line 510
- **AppContext.__repr__** (method) - Line 518
- **__getattr__** (function) - Line 528

### `C:\tmp\flask\src\flask\debughelpers.py`

- **DebugFilesKeyError.__init__** (method) - Line 28
- **DebugFilesKeyError.__str__** (method) - Line 46
- **FormDataRoutingRedirect.__init__** (method) - Line 57
- **newcls.__getitem__** (method) - Line 91
- **_dump_loader_info** (function) - Line 107

### `C:\tmp\flask\src\flask\globals.py`

- **ProxyMixin._get_current_object** (method) - Line 18
- **__getattr__** (function) - Line 65

### `C:\tmp\flask\src\flask\helpers.py`

- **stream_with_context** (function) - Line 52
- **stream_with_context** (function) - Line 58
- **_prepare_send_file_kwargs** (function) - Line 402
- **_split_blueprint_path** (function) - Line 645
- **_CollectErrors.__init__** (method) - Line 659
- **_CollectErrors.__enter__** (method) - Line 662
- **_CollectErrors.__exit__** (method) - Line 665

### `C:\tmp\flask\src\flask\json\provider.py`

- **JSONProvider.__init__** (method) - Line 38
- **JSONProvider._prepare_response_obj** (method) - Line 75
- **_default** (function) - Line 108

### `C:\tmp\flask\src\flask\json\tag.py`

- **TagDict.check** (method) - Line 103
- **TagDict.to_json** (method) - Line 110
- **TagDict.to_python** (method) - Line 114
- **PassDict.check** (method) - Line 122
- **PassDict.to_json** (method) - Line 125
- **TagTuple.check** (method) - Line 137
- **TagTuple.to_json** (method) - Line 140
- **TagTuple.to_python** (method) - Line 143
- **PassList.check** (method) - Line 150
- **PassList.to_json** (method) - Line 153
- **TagBytes.check** (method) - Line 163
- **TagBytes.to_json** (method) - Line 166
- **TagBytes.to_python** (method) - Line 169
- **TagMarkup.check** (method) - Line 181
- **TagMarkup.to_json** (method) - Line 184
- **TagMarkup.to_python** (method) - Line 187
- **TagUUID.check** (method) - Line 195
- **TagUUID.to_json** (method) - Line 198
- **TagUUID.to_python** (method) - Line 201
- **TagDateTime.check** (method) - Line 209
- **TagDateTime.to_json** (method) - Line 212
- **TagDateTime.to_python** (method) - Line 215
- **TaggedJSONSerializer.__init__** (method) - Line 249
- **TaggedJSONSerializer._untag_scan** (method) - Line 309

### `C:\tmp\flask\src\flask\sansio\app.py`

- **_make_timedelta** (function) - Line 52
- **App.__init__** (method) - Line 279
- **App._check_setup_finished** (method) - Line 410
- **App.create_jinja_environment** (method) - Line 476
- **App.debug** (method) - Line 563
- **App.add_url_rule** (method) - Line 605
- **App.template_filter** (method) - Line 664
- **App.template_filter** (method) - Line 666
- **App.template_test** (method) - Line 714
- **App.template_test** (method) - Line 716
- **App.template_global** (method) - Line 773
- **App.template_global** (method) - Line 775

### `C:\tmp\flask\src\flask\sansio\blueprints.py`

- **BlueprintSetupState.__init__** (method) - Line 41
- **Blueprint.__init__** (method) - Line 174
- **Blueprint._check_setup_finished** (method) - Line 213
- **Blueprint._merge_blueprint_funcs** (method) - Line 379
- **Blueprint.app_template_filter** (method) - Line 444

## 📄 File Documentation

### `C:\tmp\flask\src\flask\__init__.py`

**Language:** python

_No functions or classes found._

---

### `C:\tmp\flask\src\flask\__main__.py`

**Language:** python

_No functions or classes found._

---

### `C:\tmp\flask\src\flask\app.py`

**Language:** python

#### Functions

##### `_make_timedelta(value)`

**Line:** 73 | **Returns:** `timedelta | None`

**Parameters:**
- `value` (timedelta | int | None)

**Documentation:** _No documentation_

##### `remove_ctx(f)`

**Line:** 85 | **Returns:** `F`

**Parameters:**
- `f` (F)

**Documentation:** _No documentation_

##### `add_ctx(f)`

**Line:** 97 | **Returns:** `F`

**Parameters:**
- `f` (F)

**Documentation:** _No documentation_

#### Classes

##### Class: `Flask`

**Line:** 109

**Documentation:**

> The flask object implements a WSGI application and acts as the central
> object.  It is passed the name of the module or package of the
> application.  Once it is created it will act as a central registry for
> the view functions, the URL rules, template configuration and much more.
> 
> The name of the package is used to resolve resources from inside the
> package or the folder the module is contained in depending on if the
> package parameter resolves to an actual python package (a folder with
> an :file:`__init__.py` file inside) or a standard module (just a ``.py`` file).
> 
> For more information about resource loading, see :func:`open_resource`.
> 
> Usually you create a :class:`Flask` instance in your main module or
> in the :file:`__init__.py` file of your package like this::
> 
>     from flask import Flask
>     app = Flask(__name__)
> 
> .. admonition:: About the First Parameter
> 
>     The idea of the first parameter is to give Flask an idea of what
>     belongs to your application.  This name is used to find resources
>     on the filesystem, can be used by extensions to improve debugging
>     information and a lot more.
> 
>     So it's important what you provide there.  If you are using a single
>     module, `__name__` is always the correct value.  If you however are
>     using a package, it's usually recommended to hardcode the name of
>     your package there.
> 
>     For example if your application is defined in :file:`yourapplication/app.py`
>     you should create it with one of the two versions below::
> 
>         app = Flask('yourapplication')
>         app = Flask(__name__.split('.')[0])
> 
>     Why is that?  The application will work even with `__name__`, thanks
>     to how resources are looked up.  However it will make debugging more
>     painful.  Certain extensions can make assumptions based on the
>     import name of your application.  For example the Flask-SQLAlchemy
>     extension will look for the code in your application that triggered
>     an SQL query in debug mode.  If the import name is not properly set
>     up, that debugging information is lost.  (For example it would only
>     pick up SQL queries in `yourapplication.app` and not
>     `yourapplication.views.frontend`)
> 
> .. versionadded:: 0.7
>    The `static_url_path`, `static_folder`, and `template_folder`
>    parameters were added.
> 
> .. versionadded:: 0.8
>    The `instance_path` and `instance_relative_config` parameters were
>    added.
> 
> .. versionadded:: 0.11
>    The `root_path` parameter was added.
> 
> .. versionadded:: 1.0
>    The ``host_matching`` and ``static_host`` parameters were added.
> 
> .. versionadded:: 1.0
>    The ``subdomain_matching`` parameter was added. Subdomain
>    matching needs to be enabled manually now. Setting
>    :data:`SERVER_NAME` does not implicitly enable it.
> 
> :param import_name: the name of the application package
> :param static_url_path: can be used to specify a different path for the
>                         static files on the web.  Defaults to the name
>                         of the `static_folder` folder.
> :param static_folder: The folder with static files that is served at
>     ``static_url_path``. Relative to the application ``root_path``
>     or an absolute path. Defaults to ``'static'``.
> :param static_host: the host to use when adding the static route.
>     Defaults to None. Required when using ``host_matching=True``
>     with a ``static_folder`` configured.
> :param host_matching: set ``url_map.host_matching`` attribute.
>     Defaults to False.
> :param subdomain_matching: consider the subdomain relative to
>     :data:`SERVER_NAME` when matching routes. Defaults to False.
> :param template_folder: the folder that contains the templates that should
>                         be used by the application.  Defaults to
>                         ``'templates'`` folder in the root path of the
>                         application.
> :param instance_path: An alternative instance path for the application.
>                       By default the folder ``'instance'`` next to the
>                       package or module is assumed to be the instance
>                       path.
> :param instance_relative_config: if set to ``True`` relative filenames
>                                  for loading the config are assumed to
>                                  be relative to the instance path instead
>                                  of the application root.
> :param root_path: The path to the root of the application files.
>     This should only be set manually when it can't be detected
>     automatically, such as for namespace packages.

**Methods:**

- **`__init_subclass__(cls, **kwargs)`** (Line 254)
  - Returns: `None`
  - _No documentation_

- **`__init__(self, import_name, static_url_path, static_folder, static_host, host_matching, subdomain_matching, template_folder, instance_path, instance_relative_config, root_path)`** (Line 310)
  - Returns: `None`
  - _No documentation_

- **`get_send_file_max_age(self, filename)`** (Line 365)
  - Returns: `int | None`
  - Used by :func:`send_file` to determine the ``max_age`` cache

- **`send_static_file(self, filename)`** (Line 392)
  - Returns: `Response`
  - The view function used to serve files from

- **`open_resource(self, resource, mode, encoding)`** (Line 414)
  - Returns: `t.IO[t.AnyStr]`
  - Open a resource file relative to :attr:`root_path` for reading.

- **`open_instance_resource(self, resource, mode, encoding)`** (Line 447)
  - Returns: `t.IO[t.AnyStr]`
  - Open a resource file relative to the application's instance folder

- **`create_jinja_environment(self)`** (Line 469)
  - Returns: `Environment`
  - Create the Jinja environment based on :attr:`jinja_options`

- **`create_url_adapter(self, request)`** (Line 509)
  - Returns: `MapAdapter | None`
  - Creates a URL adapter for the given request. The URL adapter

- **`raise_routing_exception(self, request)`** (Line 562)
  - Returns: `t.NoReturn`
  - Intercept routing exceptions and possibly do something else.

- **`update_template_context(self, ctx, context)`** (Line 590)
  - Returns: `None`
  - Update the template context with some commonly used variables.

- **`make_shell_context(self)`** (Line 620)
  - Returns: `dict[str, t.Any]`
  - Returns the shell context for an interactive shell for this

- **`run(self, host, port, debug, load_dotenv, **options)`** (Line 632)
  - Returns: `None`
  - Runs the application on a local development server.

- **`test_client(self, use_cookies, **kwargs)`** (Line 755)
  - Returns: `FlaskClient`
  - Creates a test client for this application.  For information

- **`test_cli_runner(self, **kwargs)`** (Line 813)
  - Returns: `FlaskCliRunner`
  - Create a CLI runner for testing CLI commands.

- **`handle_http_exception(self, ctx, e)`** (Line 830)
  - Returns: `HTTPException | ft.ResponseReturnValue`
  - Handles an HTTP exception.  By default this will invoke the

- **`handle_user_exception(self, ctx, e)`** (Line 865)
  - Returns: `HTTPException | ft.ResponseReturnValue`
  - This method is called whenever an exception occurs that

- **`handle_exception(self, ctx, e)`** (Line 897)
  - Returns: `Response`
  - Handle an exception that did not have an error handler

- **`log_exception(self, ctx, exc_info)`** (Line 950)
  - Returns: `None`
  - Logs an exception.  This is called by :meth:`handle_exception`

- **`dispatch_request(self, ctx)`** (Line 966)
  - Returns: `ft.ResponseReturnValue`
  - Does the request dispatching.  Matches the URL and returns the

- **`full_dispatch_request(self, ctx)`** (Line 992)
  - Returns: `Response`
  - Dispatches the request and on top of that performs request

- **`finalize_request(self, ctx, rv, from_error_handler)`** (Line 1021)
  - Returns: `Response`
  - Given the return value from a view function this finalizes

- **`make_default_options_response(self, ctx)`** (Line 1053)
  - Returns: `Response`
  - This method is called to create the default ``OPTIONS`` response.

- **`ensure_sync(self, func)`** (Line 1065)
  - Returns: `t.Callable[..., t.Any]`
  - Ensure that the function is synchronous for WSGI workers.

- **`async_to_sync(self, func)`** (Line 1079)
  - Returns: `t.Callable[..., t.Any]`
  - Return a sync function that will run the coroutine function.

- **`url_for(endpoint, **values)`** (Line 1102)
  - Returns: `str`
  - Generate a URL to the given endpoint with the given values.

- **`make_response(self, rv)`** (Line 1224)
  - Returns: `Response`
  - Convert the return value from a view function to an instance of

- **`preprocess_request(self, ctx)`** (Line 1366)
  - Returns: `ft.ResponseReturnValue | None`
  - Called before the request is dispatched. Calls

- **`process_response(self, ctx, response)`** (Line 1394)
  - Returns: `Response`
  - Can be overridden in order to modify the response object

- **`do_teardown_request(self, ctx, exc)`** (Line 1420)
  - Returns: `None`
  - Called after the request is dispatched and the response is finalized,

- **`do_teardown_appcontext(self, ctx, exc)`** (Line 1453)
  - Returns: `None`
  - Called right before the application context is popped. Called by

- **`app_context(self)`** (Line 1481)
  - Returns: `AppContext`
  - Create an :class:`.AppContext`. When the context is pushed,

- **`request_context(self, environ)`** (Line 1501)
  - Returns: `AppContext`
  - Create an :class:`.AppContext` with request information representing

- **`test_request_context(self, *args, **kwargs)`** (Line 1517)
  - Returns: `AppContext`
  - Create an :class:`.AppContext` with request information created from

- **`wsgi_app(self, environ, start_response)`** (Line 1566)
  - Returns: `cabc.Iterable[bytes]`
  - The actual WSGI application. This is not implemented in

- **`__call__(self, environ, start_response)`** (Line 1618)
  - Returns: `cabc.Iterable[bytes]`
  - The WSGI server calls the Flask application object as the

---

### `C:\tmp\flask\src\flask\blueprints.py`

**Language:** python

#### Classes

##### Class: `Blueprint`

**Line:** 18

**Documentation:** _No documentation_

**Methods:**

- **`__init__(self, name, import_name, static_folder, static_url_path, template_folder, url_prefix, subdomain, url_defaults, root_path, cli_group)`** (Line 19)
  - Returns: `None`
  - _No documentation_

- **`get_send_file_max_age(self, filename)`** (Line 55)
  - Returns: `int | None`
  - Used by :func:`send_file` to determine the ``max_age`` cache

- **`send_static_file(self, filename)`** (Line 82)
  - Returns: `Response`
  - The view function used to serve files from

- **`open_resource(self, resource, mode, encoding)`** (Line 104)
  - Returns: `t.IO[t.AnyStr]`
  - Open a resource file relative to :attr:`root_path` for reading. The

---

### `C:\tmp\flask\src\flask\cli.py`

**Language:** python

#### Functions

##### `find_best_app(module)`

**Line:** 41 | **Returns:** `Flask`

**Parameters:**
- `module` (ModuleType)

**Documentation:**

> Given a module instance this tries to find the best possible
> application in the module or raises an exception.

##### `_called_with_wrong_args(f)`

**Line:** 94 | **Returns:** `bool`

**Parameters:**
- `f` (t.Callable[..., Flask])

**Documentation:**

> Check whether calling a function raised a ``TypeError`` because
> the call failed or because something in the factory raised the
> error.
> 
> :param f: The function that was called.
> :return: ``True`` if the call failed.

##### `find_app_by_string(module, app_name)`

**Line:** 120 | **Returns:** `Flask`

**Parameters:**
- `module` (ModuleType)
- `app_name` (str)

**Documentation:**

> Check if the given string is a variable name or a function. Call
> a function to get the app instance, or return the variable directly.

##### `prepare_import(path)`

**Line:** 200 | **Returns:** `str`

**Parameters:**
- `path` (str)

**Documentation:**

> Given a filename this will try to calculate the python path, add it
> to the search path and return the actual module name that is expected.

##### `locate_app(module_name, app_name, raise_if_not_found)`

**Line:** 230 | **Returns:** `Flask`

**Parameters:**
- `module_name` (str)
- `app_name` (str | None)
- `raise_if_not_found` (t.Literal[True])

**Documentation:** _No documentation_

##### `locate_app(module_name, app_name, raise_if_not_found)`

**Line:** 236 | **Returns:** `Flask | None`

**Parameters:**
- `module_name` (str)
- `app_name` (str | None)
- `raise_if_not_found` (t.Literal[False])

**Documentation:** _No documentation_

##### `locate_app(module_name, app_name, raise_if_not_found)`

**Line:** 241 | **Returns:** `Flask | None`

**Parameters:**
- `module_name` (str)
- `app_name` (str | None)
- `raise_if_not_found` (bool)

**Documentation:** _No documentation_

##### `get_version(ctx, param, value)`

**Line:** 267 | **Returns:** `None`

**Parameters:**
- `ctx` (click.Context)
- `param` (click.Parameter)
- `value` (t.Any)

**Documentation:** _No documentation_

##### `with_appcontext(f)`

**Line:** 380 | **Returns:** `F`

**Parameters:**
- `f` (F)

**Documentation:**

> Wraps a callback so that it's guaranteed to be executed with the
> script's application context.
> 
> Custom commands (and their options) registered under ``app.cli`` or
> ``blueprint.cli`` will always have an app context available, this
> decorator is not required in that case.
> 
> .. versionchanged:: 2.2
>     The app context is active for subcommands as well as the
>     decorated callback. The app context is always available to
>     ``app.cli`` command and parameter callbacks.

##### `_set_app(ctx, param, value)`

**Line:** 440 | **Returns:** `str | None`

**Parameters:**
- `ctx` (click.Context)
- `param` (click.Option)
- `value` (str | None)

**Documentation:** _No documentation_

##### `_set_debug(ctx, param, value)`

**Line:** 468 | **Returns:** `bool | None`

**Parameters:**
- `ctx` (click.Context)
- `param` (click.Option)
- `value` (bool)

**Documentation:** _No documentation_

##### `_env_file_callback(ctx, param, value)`

**Line:** 493 | **Returns:** `str | None`

**Parameters:**
- `ctx` (click.Context)
- `param` (click.Option)
- `value` (str | None)

**Documentation:** _No documentation_

##### `_path_is_ancestor(path, other)`

**Line:** 691 | **Returns:** `bool`

**Parameters:**
- `path` (str)
- `other` (str)

**Documentation:**

> Take ``other`` and remove the length of ``path`` from it. Then join it
> to ``path``. If it is the original value, ``path`` is an ancestor of
> ``other``.

##### `load_dotenv(path, load_defaults)`

**Line:** 698 | **Returns:** `bool`

**Parameters:**
- `path` (str | os.PathLike[str] | None)
- `load_defaults` (bool)

**Documentation:**

> Load "dotenv" files to set environment variables. A given path takes
> precedence over ``.env``, which takes precedence over ``.flaskenv``. After
> loading and combining these files, values are only set if the key is not
> already set in ``os.environ``.
> 
> This is a no-op if `python-dotenv`_ is not installed.
> 
> .. _python-dotenv: https://github.com/theskumar/python-dotenv#readme
> 
> :param path: Load the file at this location.
> :param load_defaults: Search for and load the default ``.flaskenv`` and
>     ``.env`` files.
> :return: ``True`` if at least one env var was loaded.
> 
> .. versionchanged:: 3.1
>     Added the ``load_defaults`` parameter. A given path takes precedence
>     over default files.
> 
> .. versionchanged:: 2.0
>     The current directory is not changed to the location of the
>     loaded file.
> 
> .. versionchanged:: 2.0
>     When loading the env files, set the default encoding to UTF-8.
> 
> .. versionchanged:: 1.1.0
>     Returns ``False`` when python-dotenv is not installed, or when
>     the given path isn't a file.
> 
> .. versionadded:: 1.0

##### `show_server_banner(debug, app_import_path)`

**Line:** 766 | **Returns:** `None`

**Parameters:**
- `debug` (bool)
- `app_import_path` (str | None)

**Documentation:**

> Show extra startup messages the first time the server is run,
> ignoring the reloader.

##### `_validate_key(ctx, param, value)`

**Line:** 828 | **Returns:** `t.Any`

**Parameters:**
- `ctx` (click.Context)
- `param` (click.Parameter)
- `value` (t.Any)

**Documentation:**

> The ``--key`` option must be specified when ``--cert`` is a file.
> Modifies the ``cert`` param to be a ``(cert, key)`` pair if needed.

##### `run_command(info, host, port, reload, debugger, with_threads, cert, extra_files, exclude_patterns)`

**Line:** 935 | **Returns:** `None`

**Parameters:**
- `info` (ScriptInfo)
- `host` (str)
- `port` (int)
- `reload` (bool)
- `debugger` (bool)
- `with_threads` (bool)
- `cert` (ssl.SSLContext | tuple[str, str | None] | t.Literal['adhoc'] | None)
- `extra_files` (list[str] | None)
- `exclude_patterns` (list[str] | None)

**Documentation:**

> Run a local development server.
> 
> This server is for development purposes only. It does not provide
> the stability, security, or performance of production WSGI servers.
> 
> The reloader and debugger are enabled by default with the '--debug'
> option.

##### `shell_command()`

**Line:** 1001 | **Returns:** `None`

**Documentation:**

> Run an interactive Python shell in the context of a given
> Flask application.  The application will populate the default
> namespace of this shell according to its configuration.
> 
> This is useful for executing small snippets of management code
> without having to manually configure the application.

##### `routes_command(sort, all_methods)`

**Line:** 1061 | **Returns:** `None`

**Parameters:**
- `sort` (str)
- `all_methods` (bool)

**Documentation:**

> Show all registered routes with endpoints and methods.

##### `main()`

**Line:** 1122 | **Returns:** `None`

**Documentation:** _No documentation_

#### Classes

##### Class: `NoAppException`

**Line:** 37

**Documentation:**

> Raised if an application cannot be found or loaded.

##### Class: `ScriptInfo`

**Line:** 293

**Documentation:**

> Helper object to deal with Flask applications.  This is usually not
> necessary to interface with as it's used internally in the dispatching
> to click.  In future versions of Flask this object will most likely play
> a bigger role.  Typically it's created automatically by the
> :class:`FlaskGroup` but you can also manually create it and pass it
> onwards as click object.
> 
> .. versionchanged:: 3.1
>     Added the ``load_dotenv_defaults`` parameter and attribute.

**Methods:**

- **`__init__(self, app_import_path, create_app, set_debug_flag, load_dotenv_defaults)`** (Line 305)
  - Returns: `None`
  - _No documentation_

- **`load_app(self)`** (Line 333)
  - Returns: `Flask`
  - Loads the Flask app (if not yet loaded) and returns it.  Calling

##### Class: `AppGroup`

**Line:** 405

**Documentation:**

> This works similar to a regular click :class:`~click.Group` but it
> changes the behavior of the :meth:`command` decorator so that it
> automatically wraps the functions in :func:`with_appcontext`.
> 
> Not to be confused with :class:`FlaskGroup`.

**Methods:**

- **`command(self, *args, **kwargs)`** (Line 413)
  - Returns: `t.Callable[[t.Callable[..., t.Any]], click.Command]`
  - This works exactly like the method of the same name on a regular

- **`group(self, *args, **kwargs)`** (Line 429)
  - Returns: `t.Callable[[t.Callable[..., t.Any]], click.Group]`
  - This works exactly like the method of the same name on a regular

##### Class: `FlaskGroup`

**Line:** 531

**Documentation:**

> Special subclass of the :class:`AppGroup` group that supports
> loading more commands from the configured Flask app.  Normally a
> developer does not have to interface with this class but there are
> some very advanced use cases for which it makes sense to create an
> instance of this. see :ref:`custom-scripts`.
> 
> :param add_default_commands: if this is True then the default run and
>     shell commands will be added.
> :param add_version_option: adds the ``--version`` option.
> :param create_app: an optional callback that is passed the script info and
>     returns the loaded app.
> :param load_dotenv: Load the nearest :file:`.env` and :file:`.flaskenv`
>     files to set environment variables. Will also change the working
>     directory to the directory containing the first file found.
> :param set_debug_flag: Set the app's debug flag.
> 
> .. versionchanged:: 3.1
>     ``-e path`` takes precedence over default ``.env`` and ``.flaskenv`` files.
> 
> .. versionchanged:: 2.2
>     Added the ``-A/--app``, ``--debug/--no-debug``, ``-e/--env-file`` options.
> 
> .. versionchanged:: 2.2
>     An app context is pushed when running ``app.cli`` commands, so
>     ``@with_appcontext`` is no longer required for those commands.
> 
> .. versionchanged:: 1.0
>     If installed, python-dotenv will be used to load environment variables
>     from :file:`.env` and :file:`.flaskenv` files.

**Methods:**

- **`__init__(self, add_default_commands, create_app, add_version_option, load_dotenv, set_debug_flag, **extra)`** (Line 563)
  - Returns: `None`
  - _No documentation_

- **`_load_plugin_commands(self)`** (Line 600)
  - Returns: `None`
  - _No documentation_

- **`get_command(self, ctx, name)`** (Line 609)
  - Returns: `click.Command | None`
  - _No documentation_

- **`list_commands(self, ctx)`** (Line 636)
  - Returns: `list[str]`
  - _No documentation_

- **`make_context(self, info_name, args, parent, **extra)`** (Line 657)
  - Returns: `click.Context`
  - _No documentation_

- **`parse_args(self, ctx, args)`** (Line 678)
  - Returns: `list[str]`
  - _No documentation_

##### Class: `CertParamType`

**Line:** 780

**Documentation:**

> Click option type for the ``--cert`` option. Allows either an
> existing file, the string ``'adhoc'``, or an import for a
> :class:`~ssl.SSLContext` object.

**Methods:**

- **`__init__(self)`** (Line 788)
  - Returns: `None`
  - _No documentation_

- **`convert(self, value, param, ctx)`** (Line 791)
  - Returns: `t.Any`
  - _No documentation_

##### Class: `SeparatedPathType`

**Line:** 867

**Documentation:**

> Click option type that accepts a list of values separated by the
> OS's path separator (``:``, ``;`` on Windows). Each value is
> validated as a :class:`click.Path` type.

**Methods:**

- **`convert(self, value, param, ctx)`** (Line 873)
  - Returns: `t.Any`
  - _No documentation_

---

### `C:\tmp\flask\src\flask\config.py`

**Language:** python

#### Classes

##### Class: `ConfigAttribute`

**Line:** 20

**Documentation:**

> Makes an attribute forward to the config

**Methods:**

- **`__init__(self, name, get_converter)`** (Line 23)
  - Returns: `None`
  - _No documentation_

- **`__get__(self, obj, owner)`** (Line 30)
  - Returns: `te.Self`
  - _No documentation_

- **`__get__(self, obj, owner)`** (Line 33)
  - Returns: `T`
  - _No documentation_

- **`__get__(self, obj, owner)`** (Line 35)
  - Returns: `T | te.Self`
  - _No documentation_

- **`__set__(self, obj, value)`** (Line 46)
  - Returns: `None`
  - _No documentation_

##### Class: `Config`

**Line:** 50

**Documentation:**

> Works exactly like a dict but provides ways to fill it from files
> or special dictionaries.  There are two common patterns to populate the
> config.
> 
> Either you can fill the config from a config file::
> 
>     app.config.from_pyfile('yourconfig.cfg')
> 
> Or alternatively you can define the configuration options in the
> module that calls :meth:`from_object` or provide an import path to
> a module that should be loaded.  It is also possible to tell it to
> use the same module and with that provide the configuration values
> just before the call::
> 
>     DEBUG = True
>     SECRET_KEY = 'development key'
>     app.config.from_object(__name__)
> 
> In both cases (loading from any Python file or loading from modules),
> only uppercase keys are added to the config.  This makes it possible to use
> lowercase values in the config file for temporary values that are not added
> to the config or to define the config keys in the same file that implements
> the application.
> 
> Probably the most interesting way to load configurations is from an
> environment variable pointing to a file::
> 
>     app.config.from_envvar('YOURAPPLICATION_SETTINGS')
> 
> In this case before launching the application you have to set this
> environment variable to the file you want to use.  On Linux and OS X
> use the export statement::
> 
>     export YOURAPPLICATION_SETTINGS='/path/to/config/file'
> 
> On windows use `set` instead.
> 
> :param root_path: path to which files are read relative from.  When the
>                   config object is created by the application, this is
>                   the application's :attr:`~flask.Flask.root_path`.
> :param defaults: an optional dictionary of default values

**Methods:**

- **`__init__(self, root_path, defaults)`** (Line 94)
  - Returns: `None`
  - _No documentation_

- **`from_envvar(self, variable_name, silent)`** (Line 102)
  - Returns: `bool`
  - Loads a configuration from an environment variable pointing to

- **`from_prefixed_env(self, prefix)`** (Line 126)
  - Returns: `bool`
  - Load any environment variables that start with ``FLASK_``,

- **`from_pyfile(self, filename, silent)`** (Line 187)
  - Returns: `bool`
  - Updates the values in the config from a Python file.  This function

- **`from_object(self, obj)`** (Line 218)
  - Returns: `None`
  - Updates the values from the given object.  An object can be of one

- **`from_file(self, filename, load, silent, text)`** (Line 256)
  - Returns: `bool`
  - Update the values in the config from a file that is loaded

- **`from_mapping(self, mapping, **kwargs)`** (Line 304)
  - Returns: `bool`
  - Updates the config like :meth:`update` ignoring items with

- **`get_namespace(self, namespace, lowercase, trim_namespace)`** (Line 323)
  - Returns: `dict[str, t.Any]`
  - Returns a dictionary containing a subset of configuration options

- **`__repr__(self)`** (Line 366)
  - Returns: `str`
  - _No documentation_

---

### `C:\tmp\flask\src\flask\ctx.py`

**Language:** python

#### Functions

##### `after_this_request(f)`

**Line:** 118 | **Returns:** `ft.AfterRequestCallable[t.Any]`

**Parameters:**
- `f` (ft.AfterRequestCallable[t.Any])

**Documentation:**

> Decorate a function to run after the current request. The behavior is the
> same as :meth:`.Flask.after_request`, except it only applies to the current
> request, rather than every request. Therefore, it must be used within a
> request context, rather than during setup.
> 
> .. code-block:: python
> 
>     @app.route("/")
>     def index():
>         @after_this_request
>         def add_header(response):
>             response.headers["X-Foo"] = "Parachute"
>             return response
> 
>         return "Hello, World!"
> 
> .. versionadded:: 0.9

##### `copy_current_request_context(f)`

**Line:** 154 | **Returns:** `F`

**Parameters:**
- `f` (F)

**Documentation:**

> Decorate a function to run inside the current request context. This can
> be used when starting a background task, otherwise it will not see the app
> and request objects that were active in the parent.
> 
> .. warning::
> 
>     Due to the following caveats, it is often safer (and simpler) to pass
>     the data you need when starting the task, rather than using this and
>     relying on the context objects.
> 
> In order to avoid execution switching partially though reading data, either
> read the request body (access ``form``, ``json``, ``data``, etc) before
> starting the task, or use a lock. This can be an issue when using threading,
> but shouldn't be an issue when using greenlet/gevent or asyncio.
> 
> If the task will access ``session``, be sure to do so in the parent as well
> so that the ``Vary: cookie`` header will be set. Modifying ``session`` in
> the task should be avoided, as it may execute after the response cookie has
> already been written.
> 
> .. code-block:: python
> 
>     import gevent
>     from flask import copy_current_request_context
> 
>     @app.route('/')
>     def index():
>         @copy_current_request_context
>         def do_some_work():
>             # do some work here, it can access flask.request or
>             # flask.session like you would otherwise in the view function.
>             ...
>         gevent.spawn(do_some_work)
>         return 'Regular response'
> 
> .. versionadded:: 0.10

##### `has_request_context()`

**Line:** 209 | **Returns:** `bool`

**Documentation:**

> Test if an app context is active and if it has request information.
> 
> .. code-block:: python
> 
>     from flask import has_request_context, request
> 
>     if has_request_context():
>         remote_addr = request.remote_addr
> 
> If a request context is active, the :data:`.request` and :data:`.session`
> context proxies will available and ``True``, otherwise ``False``. You can
> use that to test the data you use, rather than using this function.
> 
> .. code-block:: python
> 
>     from flask import request
> 
>     if request:
>         remote_addr = request.remote_addr
> 
> .. versionadded:: 0.7

##### `has_app_context()`

**Line:** 235 | **Returns:** `bool`

**Documentation:**

> Test if an app context is active. Unlike :func:`has_request_context`
> this can be true outside a request, such as in a CLI command.
> 
> .. code-block:: python
> 
>     from flask import has_app_context, g
> 
>     if has_app_context():
>         g.cached_data = ...
> 
> If an app context is active, the :data:`.g` and :data:`.current_app` context
> proxies will available and ``True``, otherwise ``False``. You can use that
> to test the data you use, rather than using this function.
> 
>     from flask import g
> 
>     if g:
>         g.cached_data = ...
> 
> .. versionadded:: 0.9

##### `__getattr__(name)`

**Line:** 528 | **Returns:** `t.Any`

**Parameters:**
- `name` (str)

**Documentation:** _No documentation_

#### Classes

##### Class: `_AppCtxGlobals`

**Line:** 30

**Documentation:**

> A plain object. Used as a namespace for storing data during an
> application context.
> 
> Creating an app context automatically creates this object, which is
> made available as the :data:`.g` proxy.
> 
> .. describe:: 'key' in g
> 
>     Check whether an attribute is present.
> 
>     .. versionadded:: 0.10
> 
> .. describe:: iter(g)
> 
>     Return an iterator over the attribute names.
> 
>     .. versionadded:: 0.10

**Methods:**

- **`__getattr__(self, name)`** (Line 53)
  - Returns: `t.Any`
  - _No documentation_

- **`__setattr__(self, name, value)`** (Line 59)
  - Returns: `None`
  - _No documentation_

- **`__delattr__(self, name)`** (Line 62)
  - Returns: `None`
  - _No documentation_

- **`get(self, name, default)`** (Line 68)
  - Returns: `t.Any`
  - Get an attribute by name, or a default value. Like

- **`pop(self, name, default)`** (Line 79)
  - Returns: `t.Any`
  - Get and remove an attribute by name. Like :meth:`dict.pop`.

- **`setdefault(self, name, default)`** (Line 93)
  - Returns: `t.Any`
  - Get the value of an attribute if it is present, otherwise

- **`__contains__(self, item)`** (Line 105)
  - Returns: `bool`
  - _No documentation_

- **`__iter__(self)`** (Line 108)
  - Returns: `t.Iterator[str]`
  - _No documentation_

- **`__repr__(self)`** (Line 111)
  - Returns: `str`
  - _No documentation_

##### Class: `AppContext`

**Line:** 260

**Documentation:**

> An app context contains information about an app, and about the request
> when handling a request. A context is pushed at the beginning of each
> request and CLI command, and popped at the end. The context is referred to
> as a "request context" if it has request information, and an "app context"
> if not.
> 
> Do not use this class directly. Use :meth:`.Flask.app_context` to create an
> app context if needed during setup, and :meth:`.Flask.test_request_context`
> to create a request context if needed during tests.
> 
> When the context is popped, it will evaluate all the teardown functions
> registered with :meth:`~flask.Flask.teardown_request` (if handling a
> request) then :meth:`.Flask.teardown_appcontext`.
> 
> When using the interactive debugger, the context will be restored so
> ``request`` is still accessible. Similarly, the test client can preserve the
> context after the request ends. However, teardown functions may already have
> closed some resources such as database connections, and will run again when
> the restored context is popped.
> 
> :param app: The application this context represents.
> :param request: The request data this context represents.
> :param session: The session data this context represents. If not given,
>     loaded from the request on first access.
> 
> .. versionchanged:: 3.2
>     Merged with ``RequestContext``. The ``RequestContext`` alias will be
>     removed in Flask 4.0.
> 
> .. versionchanged:: 3.2
>     A combined app and request context is pushed for every request and CLI
>     command, rather than trying to detect if an app context is already
>     pushed.
> 
> .. versionchanged:: 3.2
>     The session is loaded the first time it is accessed, rather than when
>     the context is pushed.

**Methods:**

- **`__init__(self, app)`** (Line 300)
  - Returns: `None`
  - _No documentation_

- **`from_environ()`** (Line 340)
  - Returns: `te.Self`
  - Create an app context with request data from the given WSGI environ.

- **`has_request(self)`** (Line 351)
  - Returns: `bool`
  - True if this context was created with request data.

- **`copy(self)`** (Line 355)
  - Returns: `te.Self`
  - Create a new context with the same data objects as this context. See

- **`request(self)`** (Line 371)
  - Returns: `Request`
  - The request object associated with this context. Accessed through

- **`_get_session(self)`** (Line 381)
  - Returns: `SessionMixin`
  - Open the session if it is not already open for this request context.

- **`session(self)`** (Line 396)
  - Returns: `SessionMixin`
  - The session object associated with this context. Accessed through

- **`match_request(self)`** (Line 405)
  - Returns: `None`
  - Apply routing to the current request, storing either the matched

- **`push(self)`** (Line 416)
  - Returns: `None`
  - Push this context so that it is the active context. If this is a

- **`pop(self, exc)`** (Line 446)
  - Returns: `None`
  - Pop this context so that it is no longer the active context. Then

- **`__enter__(self)`** (Line 506)
  - Returns: `te.Self`
  - _No documentation_

- **`__exit__(self, exc_type, exc_value, tb)`** (Line 510)
  - Returns: `None`
  - _No documentation_

- **`__repr__(self)`** (Line 518)
  - Returns: `str`
  - _No documentation_

---

### `C:\tmp\flask\src\flask\debughelpers.py`

**Language:** python

#### Functions

##### `attach_enctype_error_multidict(request)`

**Line:** 81 | **Returns:** `None`

**Parameters:**
- `request` (Request)

**Documentation:**

> Patch ``request.files.__getitem__`` to raise a descriptive error
> about ``enctype=multipart/form-data``.
> 
> :param request: The request to patch.
> :meta private:

##### `_dump_loader_info(loader)`

**Line:** 107 | **Returns:** `t.Iterator[str]`

**Parameters:**
- `loader` (BaseLoader)

**Documentation:** _No documentation_

##### `explain_template_loading_attempts(app, template, attempts)`

**Line:** 124 | **Returns:** `None`

**Parameters:**
- `app` (App)
- `template` (str)
- `attempts` (list[tuple[BaseLoader, Scaffold, tuple[str, str | None, t.Callable[[], bool] | None] | None]])

**Documentation:**

> This should help developers understand what failed

#### Classes

##### Class: `UnexpectedUnicodeError`

**Line:** 17

**Documentation:**

> Raised in places where we want some better error reporting for
> unexpected unicode or binary data.

##### Class: `DebugFilesKeyError`

**Line:** 23

**Documentation:**

> Raised from request.files during debugging.  The idea is that it can
> provide a better error message than just a generic KeyError/BadRequest.

**Methods:**

- **`__init__(self, request, key)`** (Line 28)
  - Returns: `None`
  - _No documentation_

- **`__str__(self)`** (Line 46)
  - Returns: `str`
  - _No documentation_

##### Class: `FormDataRoutingRedirect`

**Line:** 50

**Documentation:**

> This exception is raised in debug mode if a routing redirect
> would cause the browser to drop the method or body. This happens
> when method is not GET, HEAD or OPTIONS and the status code is not
> 307 or 308.

**Methods:**

- **`__init__(self, request)`** (Line 57)
  - Returns: `None`
  - _No documentation_

##### Class: `newcls`

**Line:** 90

**Documentation:** _No documentation_

**Methods:**

- **`__getitem__(self, key)`** (Line 91)
  - Returns: `t.Any`
  - _No documentation_

---

### `C:\tmp\flask\src\flask\globals.py`

**Language:** python

#### Functions

##### `__getattr__(name)`

**Line:** 65 | **Returns:** `t.Any`

**Parameters:**
- `name` (str)

**Documentation:** _No documentation_

#### Classes

##### Class: `ProxyMixin`

**Line:** 17

**Documentation:** _No documentation_

**Methods:**

- **`_get_current_object(self)`** (Line 18)
  - Returns: `T`
  - _No documentation_

##### Class: `FlaskProxy`

**Line:** 22

**Documentation:** _No documentation_

##### Class: `AppContextProxy`

**Line:** 24

**Documentation:** _No documentation_

##### Class: `_AppCtxGlobalsProxy`

**Line:** 26

**Documentation:** _No documentation_

##### Class: `RequestProxy`

**Line:** 28

**Documentation:** _No documentation_

##### Class: `SessionMixinProxy`

**Line:** 30

**Documentation:** _No documentation_

---

### `C:\tmp\flask\src\flask\helpers.py`

**Language:** python

#### Functions

##### `get_debug_flag()`

**Line:** 28 | **Returns:** `bool`

**Documentation:**

> Get whether debug mode should be enabled for the app, indicated by the
> :envvar:`FLASK_DEBUG` environment variable. The default is ``False``.

##### `get_load_dotenv(default)`

**Line:** 36 | **Returns:** `bool`

**Parameters:**
- `default` (bool)

**Documentation:**

> Get whether the user has disabled loading default dotenv files by
> setting :envvar:`FLASK_SKIP_DOTENV`. The default is ``True``, load
> the files.
> 
> :param default: What to return if the env var isn't set.

##### `stream_with_context(generator_or_function)`

**Line:** 52 | **Returns:** `t.Iterator[t.AnyStr]`

**Parameters:**
- `generator_or_function` (t.Iterator[t.AnyStr])

**Documentation:** _No documentation_

##### `stream_with_context(generator_or_function)`

**Line:** 58 | **Returns:** `t.Callable[[t.Iterator[t.AnyStr]], t.Iterator[t.AnyStr]]`

**Parameters:**
- `generator_or_function` (t.Callable[..., t.Iterator[t.AnyStr]])

**Documentation:** _No documentation_

##### `stream_with_context(generator_or_function)`

**Line:** 63 | **Returns:** `t.Iterator[t.AnyStr] | t.Callable[[t.Iterator[t.AnyStr]], t.Iterator[t.AnyStr]]`

**Parameters:**
- `generator_or_function` (t.Iterator[t.AnyStr] | t.Callable[..., t.Iterator[t.AnyStr]])

**Documentation:**

> Wrap a response generator function so that it runs inside the current
> request context. This keeps :data:`.request`, :data:`.session`, and :data:`.g`
> available, even though at the point the generator runs the request context
> will typically have ended.
> 
> .. warning::
> 
>     Due to the following caveat, it is often safer to pass the data you
>     need as arguments to the generator, rather than relying on the context
>     objects.
> 
> More headers cannot be sent after the body has begun. Therefore, you must
> make sure all headers are set before starting the response. In particular,
> if the generator will access ``session``, be sure to do so in the view as
> well so that the ``Vary: cookie`` header will be set. Do not modify the
> session in the generator, as the ``Set-Cookie`` header will already be sent.
> 
> Use it as a decorator on a generator function:
> 
> .. code-block:: python
> 
>     from flask import stream_with_context, request, Response
> 
>     @app.get("/stream")
>     def streamed_response():
>         @stream_with_context
>         def generate():
>             yield "Hello "
>             yield request.args["name"]
>             yield "!"
> 
>         return Response(generate())
> 
> Or use it as a wrapper around a created generator:
> 
> .. code-block:: python
> 
>     from flask import stream_with_context, request, Response
> 
>     @app.get("/stream")
>     def streamed_response():
>         def generate():
>             yield "Hello "
>             yield request.args["name"]
>             yield "!"
> 
>         return Response(stream_with_context(generate()))
> 
> .. versionadded:: 0.9

##### `make_response(*args)`

**Line:** 151 | **Returns:** `Response`

**Parameters:**
- `*args` (t.Any)

**Documentation:**

> Sometimes it is necessary to set additional headers in a view.  Because
> views do not have to return response objects but can return a value that
> is converted into a response object by Flask itself, it becomes tricky to
> add headers to it.  This function can be called instead of using a return
> and you will get a response object which you can use to attach headers.
> 
> If view looked like this and you want to add a new header::
> 
>     def index():
>         return render_template('index.html', foo=42)
> 
> You can now do something like this::
> 
>     def index():
>         response = make_response(render_template('index.html', foo=42))
>         response.headers['X-Parachutes'] = 'parachutes are cool'
>         return response
> 
> This function accepts the very same arguments you can return from a
> view function.  This for example creates a response with a 404 error
> code::
> 
>     response = make_response(render_template('not_found.html'), 404)
> 
> The other use case of this function is to force the return value of a
> view function into a response which is helpful with view
> decorators::
> 
>     response = make_response(view_function())
>     response.headers['X-Parachutes'] = 'parachutes are cool'
> 
> Internally this function does the following things:
> 
> -   if no arguments are passed, it creates a new response argument
> -   if one argument is passed, :meth:`flask.Flask.make_response`
>     is invoked with it.
> -   if more than one argument is passed, the arguments are passed
>     to the :meth:`flask.Flask.make_response` function as tuple.
> 
> .. versionadded:: 0.6

##### `url_for(endpoint, **values)`

**Line:** 200 | **Returns:** `str`

**Parameters:**
- `endpoint` (str)
- `**values` (t.Any)

**Documentation:**

> Generate a URL to the given endpoint with the given values.
> 
> This requires an active request or application context, and calls
> :meth:`current_app.url_for() <flask.Flask.url_for>`. See that method
> for full documentation.
> 
> :param endpoint: The endpoint name associated with the URL to
>     generate. If this starts with a ``.``, the current blueprint
>     name (if any) will be used.
> :param _anchor: If given, append this as ``#anchor`` to the URL.
> :param _method: If given, generate the URL associated with this
>     method for the endpoint.
> :param _scheme: If given, the URL will have this scheme if it is
>     external.
> :param _external: If given, prefer the URL to be internal (False) or
>     require it to be external (True). External URLs include the
>     scheme and domain. When not in an active request, URLs are
>     external by default.
> :param values: Values to use for the variable parts of the URL rule.
>     Unknown keys are appended as query string arguments, like
>     ``?a=b&c=d``.
> 
> .. versionchanged:: 2.2
>     Calls ``current_app.url_for``, allowing an app to override the
>     behavior.
> 
> .. versionchanged:: 0.10
>    The ``_scheme`` parameter was added.
> 
> .. versionchanged:: 0.9
>    The ``_anchor`` and ``_method`` parameters were added.
> 
> .. versionchanged:: 0.9
>    Calls ``app.handle_url_build_error`` on build errors.

##### `redirect(location, code, Response)`

**Line:** 254 | **Returns:** `BaseResponse`

**Parameters:**
- `location` (str)
- `code` (int)
- `Response` (type[BaseResponse] | None)

**Documentation:**

> Create a redirect response object.
> 
> If :data:`~flask.current_app` is available, it will use its
> :meth:`~flask.Flask.redirect` method, otherwise it will use
> :func:`werkzeug.utils.redirect`.
> 
> :param location: The URL to redirect to.
> :param code: The status code for the redirect.
> :param Response: The response class to use. Not used when
>     ``current_app`` is active, which uses ``app.response_class``.
> 
> .. versionchanged:: 3.2
>     ``code`` defaults to ``303`` instead of ``302``.
> 
> .. versionadded:: 2.2
>     Calls ``current_app.redirect`` if available instead of always
>     using Werkzeug's default ``redirect``.

##### `abort(code, *args, **kwargs)`

**Line:** 281 | **Returns:** `t.NoReturn`

**Parameters:**
- `code` (int | BaseResponse)
- `*args` (t.Any)
- `**kwargs` (t.Any)

**Documentation:**

> Raise an :exc:`~werkzeug.exceptions.HTTPException` for the given
> status code.
> 
> If :data:`~flask.current_app` is available, it will call its
> :attr:`~flask.Flask.aborter` object, otherwise it will use
> :func:`werkzeug.exceptions.abort`.
> 
> :param code: The status code for the exception, which must be
>     registered in ``app.aborter``.
> :param args: Passed to the exception.
> :param kwargs: Passed to the exception.
> 
> .. versionadded:: 2.2
>     Calls ``current_app.aborter`` if available instead of always
>     using Werkzeug's default ``abort``.

##### `get_template_attribute(template_name, attribute)`

**Line:** 304 | **Returns:** `t.Any`

**Parameters:**
- `template_name` (str)
- `attribute` (str)

**Documentation:**

> Loads a macro (or variable) a template exports.  This can be used to
> invoke a macro from within Python code.  If you for example have a
> template named :file:`_cider.html` with the following contents:
> 
> .. sourcecode:: html+jinja
> 
>    {% macro hello(name) %}Hello {{ name }}!{% endmacro %}
> 
> You can access this from Python code like this::
> 
>     hello = get_template_attribute('_cider.html', 'hello')
>     return hello('World')
> 
> .. versionadded:: 0.2
> 
> :param template_name: the name of the template
> :param attribute: the name of the variable of macro to access

##### `flash(message, category)`

**Line:** 326 | **Returns:** `None`

**Parameters:**
- `message` (str)
- `category` (str)

**Documentation:**

> Flashes a message to the next request.  In order to remove the
> flashed message from the session and to display it to the user,
> the template has to call :func:`get_flashed_messages`.
> 
> .. versionchanged:: 0.3
>    `category` parameter added.
> 
> :param message: the message to be flashed.
> :param category: the category for the message.  The following values
>                  are recommended: ``'message'`` for any kind of message,
>                  ``'error'`` for errors, ``'info'`` for information
>                  messages and ``'warning'`` for warnings.  However any
>                  kind of string can be used as category.

##### `get_flashed_messages(with_categories, category_filter)`

**Line:** 360 | **Returns:** `list[str] | list[tuple[str, str]]`

**Parameters:**
- `with_categories` (bool)
- `category_filter` (t.Iterable[str])

**Documentation:**

> Pulls all flashed messages from the session and returns them.
> Further calls in the same request to the function will return
> the same messages.  By default just the messages are returned,
> but when `with_categories` is set to ``True``, the return value will
> be a list of tuples in the form ``(category, message)`` instead.
> 
> Filter the flashed messages to one or more categories by providing those
> categories in `category_filter`.  This allows rendering categories in
> separate html blocks.  The `with_categories` and `category_filter`
> arguments are distinct:
> 
> * `with_categories` controls whether categories are returned with message
>   text (``True`` gives a tuple, where ``False`` gives just the message text).
> * `category_filter` filters the messages down to only those matching the
>   provided categories.
> 
> See :doc:`/patterns/flashing` for examples.
> 
> .. versionchanged:: 0.3
>    `with_categories` parameter added.
> 
> .. versionchanged:: 0.9
>     `category_filter` parameter added.
> 
> :param with_categories: set to ``True`` to also receive categories.
> :param category_filter: filter of categories to limit return values.  Only
>                         categories in the list will be returned.

##### `_prepare_send_file_kwargs(**kwargs)`

**Line:** 402 | **Returns:** `dict[str, t.Any]`

**Parameters:**
- `**kwargs` (t.Any)

**Documentation:** _No documentation_

##### `send_file(path_or_file, mimetype, as_attachment, download_name, conditional, etag, last_modified, max_age)`

**Line:** 417 | **Returns:** `Response`

**Parameters:**
- `path_or_file` (os.PathLike[t.AnyStr] | str | t.IO[bytes])
- `mimetype` (str | None)
- `as_attachment` (bool)
- `download_name` (str | None)
- `conditional` (bool)
- `etag` (bool | str)
- `last_modified` (datetime | int | float | None)
- `max_age` (None | (int | t.Callable[[str | None], int | None]))

**Documentation:**

> Send the contents of a file to the client.
> 
> The first argument can be a file path or a file-like object. Paths
> are preferred in most cases because Werkzeug can manage the file and
> get extra information from the path. Passing a file-like object
> requires that the file is opened in binary mode, and is mostly
> useful when building a file in memory with :class:`io.BytesIO`.
> 
> Never pass file paths provided by a user. The path is assumed to be
> trusted, so a user could craft a path to access a file you didn't
> intend. Use :func:`send_from_directory` to safely serve
> user-requested paths from within a directory.
> 
> If the WSGI server sets a ``file_wrapper`` in ``environ``, it is
> used, otherwise Werkzeug's built-in wrapper is used. Alternatively,
> if the HTTP server supports ``X-Sendfile``, configuring Flask with
> ``USE_X_SENDFILE = True`` will tell the server to send the given
> path, which is much more efficient than reading it in Python.
> 
> :param path_or_file: The path to the file to send, relative to the
>     current working directory if a relative path is given.
>     Alternatively, a file-like object opened in binary mode. Make
>     sure the file pointer is seeked to the start of the data.
> :param mimetype: The MIME type to send for the file. If not
>     provided, it will try to detect it from the file name.
> :param as_attachment: Indicate to a browser that it should offer to
>     save the file instead of displaying it.
> :param download_name: The default name browsers will use when saving
>     the file. Defaults to the passed file name.
> :param conditional: Enable conditional and range responses based on
>     request headers. Requires passing a file path and ``environ``.
> :param etag: Calculate an ETag for the file, which requires passing
>     a file path. Can also be a string to use instead.
> :param last_modified: The last modified time to send for the file,
>     in seconds. If not provided, it will try to detect it from the
>     file path.
> :param max_age: How long the client should cache the file, in
>     seconds. If set, ``Cache-Control`` will be ``public``, otherwise
>     it will be ``no-cache`` to prefer conditional caching.
> 
> .. versionchanged:: 2.0
>     ``download_name`` replaces the ``attachment_filename``
>     parameter. If ``as_attachment=False``, it is passed with
>     ``Content-Disposition: inline`` instead.
> 
> .. versionchanged:: 2.0
>     ``max_age`` replaces the ``cache_timeout`` parameter.
>     ``conditional`` is enabled and ``max_age`` is not set by
>     default.
> 
> .. versionchanged:: 2.0
>     ``etag`` replaces the ``add_etags`` parameter. It can be a
>     string to use instead of generating one.
> 
> .. versionchanged:: 2.0
>     Passing a file-like object that inherits from
>     :class:`~io.TextIOBase` will raise a :exc:`ValueError` rather
>     than sending an empty file.
> 
> .. versionadded:: 2.0
>     Moved the implementation to Werkzeug. This is now a wrapper to
>     pass some Flask-specific arguments.
> 
> .. versionchanged:: 1.1
>     ``filename`` may be a :class:`~os.PathLike` object.
> 
> .. versionchanged:: 1.1
>     Passing a :class:`~io.BytesIO` object supports range requests.
> 
> .. versionchanged:: 1.0.3
>     Filenames are encoded with ASCII instead of Latin-1 for broader
>     compatibility with WSGI servers.
> 
> .. versionchanged:: 1.0
>     UTF-8 filenames as specified in :rfc:`2231` are supported.
> 
> .. versionchanged:: 0.12
>     The filename is no longer automatically inferred from file
>     objects. If you want to use automatic MIME and etag support,
>     pass a filename via ``filename_or_fp`` or
>     ``attachment_filename``.
> 
> .. versionchanged:: 0.12
>     ``attachment_filename`` is preferred over ``filename`` for MIME
>     detection.
> 
> .. versionchanged:: 0.9
>     ``cache_timeout`` defaults to
>     :meth:`Flask.get_send_file_max_age`.
> 
> .. versionchanged:: 0.7
>     MIME guessing and etag support for file-like objects was
>     removed because it was unreliable. Pass a filename if you are
>     able to, otherwise attach an etag yourself.
> 
> .. versionchanged:: 0.5
>     The ``add_etags``, ``cache_timeout`` and ``conditional``
>     parameters were added. The default behavior is to add etags.
> 
> .. versionadded:: 0.2

##### `send_from_directory(directory, path, **kwargs)`

**Line:** 543 | **Returns:** `Response`

**Parameters:**
- `directory` (os.PathLike[str] | str)
- `path` (os.PathLike[str] | str)
- `**kwargs` (t.Any)

**Documentation:**

> Send a file from within a directory using :func:`send_file`.
> 
> .. code-block:: python
> 
>     @app.route("/uploads/<path:name>")
>     def download_file(name):
>         return send_from_directory(
>             app.config['UPLOAD_FOLDER'], name, as_attachment=True
>         )
> 
> This is a secure way to serve files from a folder, such as static
> files or uploads. Uses :func:`~werkzeug.security.safe_join` to
> ensure the path coming from the client is not maliciously crafted to
> point outside the specified directory.
> 
> If the final path does not point to an existing regular file,
> raises a 404 :exc:`~werkzeug.exceptions.NotFound` error.
> 
> :param directory: The directory that ``path`` must be located under,
>     relative to the current application's root path. This *must not*
>     be a value provided by the client, otherwise it becomes insecure.
> :param path: The path to the file to send, relative to
>     ``directory``.
> :param kwargs: Arguments to pass to :func:`send_file`.
> 
> .. versionchanged:: 2.0
>     ``path`` replaces the ``filename`` parameter.
> 
> .. versionadded:: 2.0
>     Moved the implementation to Werkzeug. This is now a wrapper to
>     pass some Flask-specific arguments.
> 
> .. versionadded:: 0.5

##### `get_root_path(import_name)`

**Line:** 587 | **Returns:** `str`

**Parameters:**
- `import_name` (str)

**Documentation:**

> Find the root path of a package, or the path that contains a
> module. If it cannot be found, returns the current working
> directory.
> 
> Not to be confused with the value returned by :func:`find_package`.
> 
> :meta private:

##### `_split_blueprint_path(name)`

**Line:** 645 | **Returns:** `list[str]`

**Parameters:**
- `name` (str)

**Documentation:** _No documentation_

#### Classes

##### Class: `_CollectErrors`

**Line:** 654

**Documentation:**

> A context manager that records and silences an error raised within it.
> Used to run all teardown functions, then raise any errors afterward.

**Methods:**

- **`__init__(self)`** (Line 659)
  - Returns: `None`
  - _No documentation_

- **`__enter__(self)`** (Line 662)
  - Returns: `None`
  - _No documentation_

- **`__exit__(self, exc_type, exc_val, exc_tb)`** (Line 665)
  - Returns: `bool`
  - _No documentation_

- **`raise_any(self, message)`** (Line 676)
  - Returns: `None`
  - Raise if any errors were collected.

---

### `C:\tmp\flask\src\flask\json\__init__.py`

**Language:** python

#### Functions

##### `dumps(obj, **kwargs)`

**Line:** 13 | **Returns:** `str`

**Parameters:**
- `obj` (t.Any)
- `**kwargs` (t.Any)

**Documentation:**

> Serialize data as JSON.
> 
> If :data:`~flask.current_app` is available, it will use its
> :meth:`app.json.dumps() <flask.json.provider.JSONProvider.dumps>`
> method, otherwise it will use :func:`json.dumps`.
> 
> :param obj: The data to serialize.
> :param kwargs: Arguments passed to the ``dumps`` implementation.
> 
> .. versionchanged:: 2.3
>     The ``app`` parameter was removed.
> 
> .. versionchanged:: 2.2
>     Calls ``current_app.json.dumps``, allowing an app to override
>     the behavior.
> 
> .. versionchanged:: 2.0.2
>     :class:`decimal.Decimal` is supported by converting to a string.
> 
> .. versionchanged:: 2.0
>     ``encoding`` will be removed in Flask 2.1.
> 
> .. versionchanged:: 1.0.3
>     ``app`` can be passed directly, rather than requiring an app
>     context for configuration.

##### `dump(obj, fp, **kwargs)`

**Line:** 47 | **Returns:** `None`

**Parameters:**
- `obj` (t.Any)
- `fp` (t.IO[str])
- `**kwargs` (t.Any)

**Documentation:**

> Serialize data as JSON and write to a file.
> 
> If :data:`~flask.current_app` is available, it will use its
> :meth:`app.json.dump() <flask.json.provider.JSONProvider.dump>`
> method, otherwise it will use :func:`json.dump`.
> 
> :param obj: The data to serialize.
> :param fp: A file opened for writing text. Should use the UTF-8
>     encoding to be valid JSON.
> :param kwargs: Arguments passed to the ``dump`` implementation.
> 
> .. versionchanged:: 2.3
>     The ``app`` parameter was removed.
> 
> .. versionchanged:: 2.2
>     Calls ``current_app.json.dump``, allowing an app to override
>     the behavior.
> 
> .. versionchanged:: 2.0
>     Writing to a binary file, and the ``encoding`` argument, will be
>     removed in Flask 2.1.

##### `loads(s, **kwargs)`

**Line:** 77 | **Returns:** `t.Any`

**Parameters:**
- `s` (str | bytes)
- `**kwargs` (t.Any)

**Documentation:**

> Deserialize data as JSON.
> 
> If :data:`~flask.current_app` is available, it will use its
> :meth:`app.json.loads() <flask.json.provider.JSONProvider.loads>`
> method, otherwise it will use :func:`json.loads`.
> 
> :param s: Text or UTF-8 bytes.
> :param kwargs: Arguments passed to the ``loads`` implementation.
> 
> .. versionchanged:: 2.3
>     The ``app`` parameter was removed.
> 
> .. versionchanged:: 2.2
>     Calls ``current_app.json.loads``, allowing an app to override
>     the behavior.
> 
> .. versionchanged:: 2.0
>     ``encoding`` will be removed in Flask 2.1. The data must be a
>     string or UTF-8 bytes.
> 
> .. versionchanged:: 1.0.3
>     ``app`` can be passed directly, rather than requiring an app
>     context for configuration.

##### `load(fp, **kwargs)`

**Line:** 108 | **Returns:** `t.Any`

**Parameters:**
- `fp` (t.IO[t.AnyStr])
- `**kwargs` (t.Any)

**Documentation:**

> Deserialize data as JSON read from a file.
> 
> If :data:`~flask.current_app` is available, it will use its
> :meth:`app.json.load() <flask.json.provider.JSONProvider.load>`
> method, otherwise it will use :func:`json.load`.
> 
> :param fp: A file opened for reading text or UTF-8 bytes.
> :param kwargs: Arguments passed to the ``load`` implementation.
> 
> .. versionchanged:: 2.3
>     The ``app`` parameter was removed.
> 
> .. versionchanged:: 2.2
>     Calls ``current_app.json.load``, allowing an app to override
>     the behavior.
> 
> .. versionchanged:: 2.2
>     The ``app`` parameter will be removed in Flask 2.3.
> 
> .. versionchanged:: 2.0
>     ``encoding`` will be removed in Flask 2.1. The file must be text
>     mode, or binary mode with UTF-8 bytes.

##### `jsonify(*args, **kwargs)`

**Line:** 138 | **Returns:** `Response`

**Parameters:**
- `*args` (t.Any)
- `**kwargs` (t.Any)

**Documentation:**

> Serialize the given arguments as JSON, and return a
> :class:`~flask.Response` object with the ``application/json``
> mimetype. A dict or list returned from a view will be converted to a
> JSON response automatically without needing to call this.
> 
> This requires an active app context, and calls
> :meth:`app.json.response() <flask.json.provider.JSONProvider.response>`.
> 
> In debug mode, the output is formatted with indentation to make it
> easier to read. This may also be controlled by the provider.
> 
> Either positional or keyword arguments can be given, not both.
> If no arguments are given, ``None`` is serialized.
> 
> :param args: A single value to serialize, or multiple values to
>     treat as a list to serialize.
> :param kwargs: Treat as a dict to serialize.
> 
> .. versionchanged:: 2.2
>     Calls ``current_app.json.response``, allowing an app to override
>     the behavior.
> 
> .. versionchanged:: 2.0.2
>     :class:`decimal.Decimal` is supported by converting to a string.
> 
> .. versionchanged:: 0.11
>     Added support for serializing top-level arrays. This was a
>     security risk in ancient browsers. See :ref:`security-json`.
> 
> .. versionadded:: 0.2

---

### `C:\tmp\flask\src\flask\json\provider.py`

**Language:** python

#### Functions

##### `_default(o)`

**Line:** 108 | **Returns:** `t.Any`

**Parameters:**
- `o` (t.Any)

**Documentation:** _No documentation_

#### Classes

##### Class: `JSONProvider`

**Line:** 19

**Documentation:**

> A standard set of JSON operations for an application. Subclasses
> of this can be used to customize JSON behavior or use different
> JSON libraries.
> 
> To implement a provider for a specific library, subclass this base
> class and implement at least :meth:`dumps` and :meth:`loads`. All
> other methods have default implementations.
> 
> To use a different provider, either subclass ``Flask`` and set
> :attr:`~flask.Flask.json_provider_class` to a provider class, or set
> :attr:`app.json <flask.Flask.json>` to an instance of the class.
> 
> :param app: An application instance. This will be stored as a
>     :class:`weakref.proxy` on the :attr:`_app` attribute.
> 
> .. versionadded:: 2.2

**Methods:**

- **`__init__(self, app)`** (Line 38)
  - Returns: `None`
  - _No documentation_

- **`dumps(self, obj, **kwargs)`** (Line 41)
  - Returns: `str`
  - Serialize data as JSON.

- **`dump(self, obj, fp, **kwargs)`** (Line 49)
  - Returns: `None`
  - Serialize data as JSON and write to a file.

- **`loads(self, s, **kwargs)`** (Line 59)
  - Returns: `t.Any`
  - Deserialize data as JSON.

- **`load(self, fp, **kwargs)`** (Line 67)
  - Returns: `t.Any`
  - Deserialize data as JSON read from a file.

- **`_prepare_response_obj(self, args, kwargs)`** (Line 75)
  - Returns: `t.Any`
  - _No documentation_

- **`response(self, *args, **kwargs)`** (Line 89)
  - Returns: `Response`
  - Serialize the given arguments as JSON, and return a

##### Class: `DefaultJSONProvider`

**Line:** 124

**Documentation:**

> Provide JSON operations using Python's built-in :mod:`json`
> library. Serializes the following additional data types:
> 
> -   :class:`datetime.datetime` and :class:`datetime.date` are
>     serialized to :rfc:`822` strings. This is the same as the HTTP
>     date format.
> -   :class:`uuid.UUID` is serialized to a string.
> -   :class:`dataclasses.dataclass` is passed to
>     :func:`dataclasses.asdict`.
> -   :class:`~markupsafe.Markup` (or any object with a ``__html__``
>     method) will call the ``__html__`` method to get a string.

**Methods:**

- **`dumps(self, obj, **kwargs)`** (Line 166)
  - Returns: `str`
  - Serialize data as JSON to a string.

- **`loads(self, s, **kwargs)`** (Line 181)
  - Returns: `t.Any`
  - Deserialize data as JSON from a string or bytes.

- **`response(self, *args, **kwargs)`** (Line 189)
  - Returns: `Response`
  - Serialize the given arguments as JSON, and return a

---

### `C:\tmp\flask\src\flask\json\tag.py`

**Language:** python

#### Classes

##### Class: `JSONTag`

**Line:** 60

**Documentation:**

> Base class for defining type tags for :class:`TaggedJSONSerializer`.

**Methods:**

- **`__init__(self, serializer)`** (Line 69)
  - Returns: `None`
  - Create a tagger for the given serializer.

- **`check(self, value)`** (Line 73)
  - Returns: `bool`
  - Check if the given value should be tagged by this tag.

- **`to_json(self, value)`** (Line 77)
  - Returns: `t.Any`
  - Convert the Python object to an object that is a valid JSON type.

- **`to_python(self, value)`** (Line 82)
  - Returns: `t.Any`
  - Convert the JSON representation back to the correct type. The tag

- **`tag(self, value)`** (Line 87)
  - Returns: `dict[str, t.Any]`
  - Convert the value to a valid JSON type and add the tag structure

##### Class: `TagDict`

**Line:** 93

**Documentation:**

> Tag for 1-item dicts whose only key matches a registered tag.
> 
> Internally, the dict key is suffixed with `__`, and the suffix is removed
> when deserializing.

**Methods:**

- **`check(self, value)`** (Line 103)
  - Returns: `bool`
  - _No documentation_

- **`to_json(self, value)`** (Line 110)
  - Returns: `t.Any`
  - _No documentation_

- **`to_python(self, value)`** (Line 114)
  - Returns: `t.Any`
  - _No documentation_

##### Class: `PassDict`

**Line:** 119

**Documentation:** _No documentation_

**Methods:**

- **`check(self, value)`** (Line 122)
  - Returns: `bool`
  - _No documentation_

- **`to_json(self, value)`** (Line 125)
  - Returns: `t.Any`
  - _No documentation_

##### Class: `TagTuple`

**Line:** 133

**Documentation:** _No documentation_

**Methods:**

- **`check(self, value)`** (Line 137)
  - Returns: `bool`
  - _No documentation_

- **`to_json(self, value)`** (Line 140)
  - Returns: `t.Any`
  - _No documentation_

- **`to_python(self, value)`** (Line 143)
  - Returns: `t.Any`
  - _No documentation_

##### Class: `PassList`

**Line:** 147

**Documentation:** _No documentation_

**Methods:**

- **`check(self, value)`** (Line 150)
  - Returns: `bool`
  - _No documentation_

- **`to_json(self, value)`** (Line 153)
  - Returns: `t.Any`
  - _No documentation_

##### Class: `TagBytes`

**Line:** 159

**Documentation:** _No documentation_

**Methods:**

- **`check(self, value)`** (Line 163)
  - Returns: `bool`
  - _No documentation_

- **`to_json(self, value)`** (Line 166)
  - Returns: `t.Any`
  - _No documentation_

- **`to_python(self, value)`** (Line 169)
  - Returns: `t.Any`
  - _No documentation_

##### Class: `TagMarkup`

**Line:** 173

**Documentation:**

> Serialize anything matching the :class:`~markupsafe.Markup` API by
> having a ``__html__`` method to the result of that method. Always
> deserializes to an instance of :class:`~markupsafe.Markup`.

**Methods:**

- **`check(self, value)`** (Line 181)
  - Returns: `bool`
  - _No documentation_

- **`to_json(self, value)`** (Line 184)
  - Returns: `t.Any`
  - _No documentation_

- **`to_python(self, value)`** (Line 187)
  - Returns: `t.Any`
  - _No documentation_

##### Class: `TagUUID`

**Line:** 191

**Documentation:** _No documentation_

**Methods:**

- **`check(self, value)`** (Line 195)
  - Returns: `bool`
  - _No documentation_

- **`to_json(self, value)`** (Line 198)
  - Returns: `t.Any`
  - _No documentation_

- **`to_python(self, value)`** (Line 201)
  - Returns: `t.Any`
  - _No documentation_

##### Class: `TagDateTime`

**Line:** 205

**Documentation:** _No documentation_

**Methods:**

- **`check(self, value)`** (Line 209)
  - Returns: `bool`
  - _No documentation_

- **`to_json(self, value)`** (Line 212)
  - Returns: `t.Any`
  - _No documentation_

- **`to_python(self, value)`** (Line 215)
  - Returns: `t.Any`
  - _No documentation_

##### Class: `TaggedJSONSerializer`

**Line:** 219

**Documentation:**

> Serializer that uses a tag system to compactly represent objects that
> are not JSON types. Passed as the intermediate serializer to
> :class:`itsdangerous.Serializer`.
> 
> The following extra types are supported:
> 
> * :class:`dict`
> * :class:`tuple`
> * :class:`bytes`
> * :class:`~markupsafe.Markup`
> * :class:`~uuid.UUID`
> * :class:`~datetime.datetime`

**Methods:**

- **`__init__(self)`** (Line 249)
  - Returns: `None`
  - _No documentation_

- **`register(self, tag_class, force, index)`** (Line 256)
  - Returns: `None`
  - Register a new tag with this serializer.

- **`tag(self, value)`** (Line 289)
  - Returns: `t.Any`
  - Convert a value to a tagged representation if necessary.

- **`untag(self, value)`** (Line 297)
  - Returns: `t.Any`
  - Convert a tagged representation back to the original type.

- **`_untag_scan(self, value)`** (Line 309)
  - Returns: `t.Any`
  - _No documentation_

- **`dumps(self, value)`** (Line 321)
  - Returns: `str`
  - Tag the value and dump it to a compact JSON string.

- **`loads(self, value)`** (Line 325)
  - Returns: `t.Any`
  - Load data from a JSON string and deserialized any tagged objects.

---

### `C:\tmp\flask\src\flask\logging.py`

**Language:** python

#### Functions

##### `wsgi_errors_stream()`

**Line:** 16 | **Returns:** `t.TextIO`

**Documentation:**

> Find the most appropriate error stream for the application. If a request
> is active, log to ``wsgi.errors``, otherwise use ``sys.stderr``.
> 
> If you configure your own :class:`logging.StreamHandler`, you may want to
> use this for the stream. If you are using file or dict configuration and
> can't import this directly, you can refer to it as
> ``ext://flask.logging.wsgi_errors_stream``.

##### `has_level_handler(logger)`

**Line:** 31 | **Returns:** `bool`

**Parameters:**
- `logger` (logging.Logger)

**Documentation:**

> Check if there is a handler in the logging chain that will handle the
> given logger's :meth:`effective level <~logging.Logger.getEffectiveLevel>`.

##### `create_logger(app)`

**Line:** 58 | **Returns:** `logging.Logger`

**Parameters:**
- `app` (App)

**Documentation:**

> Get the Flask app's logger and configure it if needed.
> 
> The logger name will be the same as
> :attr:`app.import_name <flask.Flask.name>`.
> 
> When :attr:`~flask.Flask.debug` is enabled, set the logger level to
> :data:`logging.DEBUG` if it is not set.
> 
> If there is no handler for the logger's effective level, add a
> :class:`~logging.StreamHandler` for
> :func:`~flask.logging.wsgi_errors_stream` with a basic format.

---

### `C:\tmp\flask\src\flask\sansio\app.py`

**Language:** python

#### Functions

##### `_make_timedelta(value)`

**Line:** 52 | **Returns:** `timedelta | None`

**Parameters:**
- `value` (timedelta | int | None)

**Documentation:** _No documentation_

#### Classes

##### Class: `App`

**Line:** 59

**Documentation:**

> The flask object implements a WSGI application and acts as the central
> object.  It is passed the name of the module or package of the
> application.  Once it is created it will act as a central registry for
> the view functions, the URL rules, template configuration and much more.
> 
> The name of the package is used to resolve resources from inside the
> package or the folder the module is contained in depending on if the
> package parameter resolves to an actual python package (a folder with
> an :file:`__init__.py` file inside) or a standard module (just a ``.py`` file).
> 
> For more information about resource loading, see :func:`open_resource`.
> 
> Usually you create a :class:`Flask` instance in your main module or
> in the :file:`__init__.py` file of your package like this::
> 
>     from flask import Flask
>     app = Flask(__name__)
> 
> .. admonition:: About the First Parameter
> 
>     The idea of the first parameter is to give Flask an idea of what
>     belongs to your application.  This name is used to find resources
>     on the filesystem, can be used by extensions to improve debugging
>     information and a lot more.
> 
>     So it's important what you provide there.  If you are using a single
>     module, `__name__` is always the correct value.  If you however are
>     using a package, it's usually recommended to hardcode the name of
>     your package there.
> 
>     For example if your application is defined in :file:`yourapplication/app.py`
>     you should create it with one of the two versions below::
> 
>         app = Flask('yourapplication')
>         app = Flask(__name__.split('.')[0])
> 
>     Why is that?  The application will work even with `__name__`, thanks
>     to how resources are looked up.  However it will make debugging more
>     painful.  Certain extensions can make assumptions based on the
>     import name of your application.  For example the Flask-SQLAlchemy
>     extension will look for the code in your application that triggered
>     an SQL query in debug mode.  If the import name is not properly set
>     up, that debugging information is lost.  (For example it would only
>     pick up SQL queries in `yourapplication.app` and not
>     `yourapplication.views.frontend`)
> 
> .. versionadded:: 0.7
>    The `static_url_path`, `static_folder`, and `template_folder`
>    parameters were added.
> 
> .. versionadded:: 0.8
>    The `instance_path` and `instance_relative_config` parameters were
>    added.
> 
> .. versionadded:: 0.11
>    The `root_path` parameter was added.
> 
> .. versionadded:: 1.0
>    The ``host_matching`` and ``static_host`` parameters were added.
> 
> .. versionadded:: 1.0
>    The ``subdomain_matching`` parameter was added. Subdomain
>    matching needs to be enabled manually now. Setting
>    :data:`SERVER_NAME` does not implicitly enable it.
> 
> :param import_name: the name of the application package
> :param static_url_path: can be used to specify a different path for the
>                         static files on the web.  Defaults to the name
>                         of the `static_folder` folder.
> :param static_folder: The folder with static files that is served at
>     ``static_url_path``. Relative to the application ``root_path``
>     or an absolute path. Defaults to ``'static'``.
> :param static_host: the host to use when adding the static route.
>     Defaults to None. Required when using ``host_matching=True``
>     with a ``static_folder`` configured.
> :param host_matching: set ``url_map.host_matching`` attribute.
>     Defaults to False.
> :param subdomain_matching: consider the subdomain relative to
>     :data:`SERVER_NAME` when matching routes. Defaults to False.
> :param template_folder: the folder that contains the templates that should
>                         be used by the application.  Defaults to
>                         ``'templates'`` folder in the root path of the
>                         application.
> :param instance_path: An alternative instance path for the application.
>                       By default the folder ``'instance'`` next to the
>                       package or module is assumed to be the instance
>                       path.
> :param instance_relative_config: if set to ``True`` relative filenames
>                                  for loading the config are assumed to
>                                  be relative to the instance path instead
>                                  of the application root.
> :param root_path: The path to the root of the application files.
>     This should only be set manually when it can't be detected
>     automatically, such as for namespace packages.

**Methods:**

- **`__init__(self, import_name, static_url_path, static_folder, static_host, host_matching, subdomain_matching, template_folder, instance_path, instance_relative_config, root_path)`** (Line 279)
  - Returns: `None`
  - _No documentation_

- **`_check_setup_finished(self, f_name)`** (Line 410)
  - Returns: `None`
  - _No documentation_

- **`name(self)`** (Line 423)
  - Returns: `str`
  - The name of the application.  This is usually the import name

- **`logger(self)`** (Line 440)
  - Returns: `logging.Logger`
  - A standard Python :class:`~logging.Logger` for the app, with

- **`jinja_env(self)`** (Line 467)
  - Returns: `Environment`
  - The Jinja environment used to load templates.

- **`create_jinja_environment(self)`** (Line 476)
  - Returns: `Environment`
  - _No documentation_

- **`make_config(self, instance_relative)`** (Line 479)
  - Returns: `Config`
  - Used to create the config attribute by the Flask constructor.

- **`make_aborter(self)`** (Line 495)
  - Returns: `Aborter`
  - Create the object to assign to :attr:`aborter`. That object

- **`auto_find_instance_path(self)`** (Line 507)
  - Returns: `str`
  - Tries to locate the instance path if it was not provided to the

- **`create_global_jinja_loader(self)`** (Line 520)
  - Returns: `DispatchingJinjaLoader`
  - Creates the loader for the Jinja environment.  Can be used to

- **`select_jinja_autoescape(self, filename)`** (Line 533)
  - Returns: `bool`
  - Returns ``True`` if autoescaping should be active for the given

- **`debug(self)`** (Line 550)
  - Returns: `bool`
  - Whether debug mode is enabled. When using ``flask run`` to start the

- **`debug(self, value)`** (Line 563)
  - Returns: `None`
  - _No documentation_

- **`register_blueprint(self, blueprint, **options)`** (Line 570)
  - Returns: `None`
  - Register a :class:`~flask.Blueprint` on the application. Keyword

- **`iter_blueprints(self)`** (Line 597)
  - Returns: `t.ValuesView[Blueprint]`
  - Iterates over all blueprints by the order they were registered.

- **`add_url_rule(self, rule, endpoint, view_func, provide_automatic_options, **options)`** (Line 605)
  - Returns: `None`
  - _No documentation_

- **`template_filter(self, name)`** (Line 664)
  - Returns: `T_template_filter`
  - _No documentation_

- **`template_filter(self, name)`** (Line 666)
  - Returns: `t.Callable[[T_template_filter], T_template_filter]`
  - _No documentation_

- **`template_filter(self, name)`** (Line 670)
  - Returns: `T_template_filter | t.Callable[[T_template_filter], T_template_filter]`
  - Decorate a function to register it as a custom Jinja filter. The name

- **`add_template_filter(self, f, name)`** (Line 699)
  - Returns: `None`
  - Register a function to use as a custom Jinja filter.

- **`template_test(self, name)`** (Line 714)
  - Returns: `T_template_test`
  - _No documentation_

- **`template_test(self, name)`** (Line 716)
  - Returns: `t.Callable[[T_template_test], T_template_test]`
  - _No documentation_

- **`template_test(self, name)`** (Line 720)
  - Returns: `T_template_test | t.Callable[[T_template_test], T_template_test]`
  - Decorate a function to register it as a custom Jinja test. The name

- **`add_template_test(self, f, name)`** (Line 756)
  - Returns: `None`
  - Register a function to use as a custom Jinja test.

- **`template_global(self, name)`** (Line 773)
  - Returns: `T_template_global`
  - _No documentation_

- **`template_global(self, name)`** (Line 775)
  - Returns: `t.Callable[[T_template_global], T_template_global]`
  - _No documentation_

- **`template_global(self, name)`** (Line 779)
  - Returns: `T_template_global | t.Callable[[T_template_global], T_template_global]`
  - Decorate a function to register it as a custom Jinja global. The name

- **`add_template_global(self, f, name)`** (Line 810)
  - Returns: `None`
  - Register a function to use as a custom Jinja global.

- **`teardown_appcontext(self, f)`** (Line 827)
  - Returns: `T_teardown`
  - Registers a function to be called when the app context is popped. The

- **`shell_context_processor(self, f)`** (Line 858)
  - Returns: `T_shell_context_processor`
  - Registers a shell context processor function.

- **`_find_error_handler(self, e, blueprints)`** (Line 868)
  - Returns: `ft.ErrorHandlerCallable | None`
  - Return a registered error handler for an exception in this order:

- **`trap_http_exception(self, e)`** (Line 893)
  - Returns: `bool`
  - Checks if an HTTP exception should be trapped or not.  By default

- **`redirect(self, location, code)`** (Line 939)
  - Returns: `BaseResponse`
  - Create a redirect response object.

- **`inject_url_defaults(self, endpoint, values)`** (Line 960)
  - Returns: `None`
  - Injects the URL defaults for the given endpoint directly into

- **`handle_url_build_error(self, error, endpoint, values)`** (Line 981)
  - Returns: `str`
  - Called by :meth:`.url_for` if a

---

### `C:\tmp\flask\src\flask\sansio\blueprints.py`

**Language:** python

#### Classes

##### Class: `BlueprintSetupState`

**Line:** 34

**Documentation:**

> Temporary holder object for registering a blueprint with the
> application.  An instance of this class is created by the
> :meth:`~flask.Blueprint.make_setup_state` method and later passed
> to all register callback functions.

**Methods:**

- **`__init__(self, blueprint, app, options, first_registration)`** (Line 41)
  - Returns: `None`
  - _No documentation_

- **`add_url_rule(self, rule, endpoint, view_func, **options)`** (Line 87)
  - Returns: `None`
  - A helper method to register a rule (and optionally a view function)

##### Class: `Blueprint`

**Line:** 119

**Documentation:**

> Represents a blueprint, a collection of routes and other
> app-related functions that can be registered on a real application
> later.
> 
> A blueprint is an object that allows defining application functions
> without requiring an application object ahead of time. It uses the
> same decorators as :class:`~flask.Flask`, but defers the need for an
> application by recording them for later registration.
> 
> Decorating a function with a blueprint creates a deferred function
> that is called with :class:`~flask.blueprints.BlueprintSetupState`
> when the blueprint is registered on an application.
> 
> See :doc:`/blueprints` for more information.
> 
> :param name: The name of the blueprint. Will be prepended to each
>     endpoint name.
> :param import_name: The name of the blueprint package, usually
>     ``__name__``. This helps locate the ``root_path`` for the
>     blueprint.
> :param static_folder: A folder with static files that should be
>     served by the blueprint's static route. The path is relative to
>     the blueprint's root path. Blueprint static files are disabled
>     by default.
> :param static_url_path: The url to serve static files from.
>     Defaults to ``static_folder``. If the blueprint does not have
>     a ``url_prefix``, the app's static route will take precedence,
>     and the blueprint's static files won't be accessible.
> :param template_folder: A folder with templates that should be added
>     to the app's template search path. The path is relative to the
>     blueprint's root path. Blueprint templates are disabled by
>     default. Blueprint templates have a lower precedence than those
>     in the app's templates folder.
> :param url_prefix: A path to prepend to all of the blueprint's URLs,
>     to make them distinct from the rest of the app's routes.
> :param subdomain: A subdomain that blueprint routes will match on by
>     default.
> :param url_defaults: A dict of default values that blueprint routes
>     will receive by default.
> :param root_path: By default, the blueprint will automatically set
>     this based on ``import_name``. In certain situations this
>     automatic detection can fail, so the path can be specified
>     manually instead.
> 
> .. versionchanged:: 1.1.0
>     Blueprints have a ``cli`` group to register nested CLI commands.
>     The ``cli_group`` parameter controls the name of the group under
>     the ``flask`` command.
> 
> .. versionadded:: 0.7

**Methods:**

- **`__init__(self, name, import_name, static_folder, static_url_path, template_folder, url_prefix, subdomain, url_defaults, root_path, cli_group)`** (Line 174)
  - Returns: `None`
  - _No documentation_

- **`_check_setup_finished(self, f_name)`** (Line 213)
  - Returns: `None`
  - _No documentation_

- **`record(self, func)`** (Line 224)
  - Returns: `None`
  - Registers a function that is called when the blueprint is

- **`record_once(self, func)`** (Line 233)
  - Returns: `None`
  - Works like :meth:`record` but wraps the function in another

- **`make_setup_state(self, app, options, first_registration)`** (Line 246)
  - Returns: `BlueprintSetupState`
  - Creates an instance of :meth:`~flask.blueprints.BlueprintSetupState`

- **`register_blueprint(self, blueprint, **options)`** (Line 256)
  - Returns: `None`
  - Register a :class:`~flask.Blueprint` on this blueprint. Keyword

- **`register(self, app, options)`** (Line 273)
  - Returns: `None`
  - Called by :meth:`Flask.register_blueprint` to register all

- **`_merge_blueprint_funcs(self, app, name)`** (Line 379)
  - Returns: `None`
  - _No documentation_

- **`add_url_rule(self, rule, endpoint, view_func, provide_automatic_options, **options)`** (Line 413)
  - Returns: `None`
  - Register a URL rule with the blueprint. See :meth:`.Flask.add_url_rule` for

- **`app_template_filter(self, name)`** (Line 444)
  - Returns: `T_template_filter`
  - _No documentation_

- **`app_template_filter(self, name)`** (Line 446)
  - Returns: `t.Callable[[T_template_filter], T_template_filter]`
  - _No documentation_

- **`app_template_filter(self, name)`** (Line 450)
  - Returns: `T_template_filter | t.Callable[[T_template_filter], T_template_filter]`
  - Decorate a function to register it as a custom Jinja filter. The name

- **`add_app_template_filter(self, f, name)`** (Line 476)
  - Returns: `None`
  - Register a function to use as a custom Jinja filter.

- **`app_template_test(self, name)`** (Line 498)
  - Returns: `T_template_test`
  - _No documentation_

- **`app_template_test(self, name)`** (Line 500)
  - Returns: `t.Callable[[T_template_test], T_template_test]`
  - _No documentation_

- **`app_template_test(self, name)`** (Line 504)
  - Returns: `T_template_test | t.Callable[[T_template_test], T_template_test]`
  - Decorate a function to register it as a custom Jinja test. The name

- **`add_app_template_test(self, f, name)`** (Line 532)
  - Returns: `None`
  - Register a function to use as a custom Jinja test.

- **`app_template_global(self, name)`** (Line 556)
  - Returns: `T_template_global`
  - _No documentation_

- **`app_template_global(self, name)`** (Line 558)
  - Returns: `t.Callable[[T_template_global], T_template_global]`
  - _No documentation_

- **`app_template_global(self, name)`** (Line 562)
  - Returns: `T_template_global | t.Callable[[T_template_global], T_template_global]`
  - Decorate a function to register it as a custom Jinja global. The name

- **`add_app_template_global(self, f, name)`** (Line 590)
  - Returns: `None`
  - Register a function to use as a custom Jinja global.

- **`before_app_request(self, f)`** (Line 614)
  - Returns: `T_before_request`
  - Like :meth:`before_request`, but before every request, not only those handled

- **`after_app_request(self, f)`** (Line 624)
  - Returns: `T_after_request`
  - Like :meth:`after_request`, but after every request, not only those handled

- **`teardown_app_request(self, f)`** (Line 634)
  - Returns: `T_teardown`
  - Like :meth:`teardown_request`, but after every request, not only those

- **`app_context_processor(self, f)`** (Line 644)
  - Returns: `T_template_context_processor`
  - Like :meth:`context_processor`, but for templates rendered by every view, not

- **`app_errorhandler(self, code)`** (Line 656)
  - Returns: `t.Callable[[T_error_handler], T_error_handler]`
  - Like :meth:`errorhandler`, but for every request, not only those handled by

- **`app_url_value_preprocessor(self, f)`** (Line 673)
  - Returns: `T_url_value_preprocessor`
  - Like :meth:`url_value_preprocessor`, but for every request, not only those

- **`app_url_defaults(self, f)`** (Line 685)
  - Returns: `T_url_defaults`
  - Like :meth:`url_defaults`, but for every request, not only those handled by

---

### `C:\tmp\flask\src\flask\sansio\scaffold.py`

**Language:** python

#### Functions

##### `setupmethod(f)`

**Line:** 42 | **Returns:** `F`

**Parameters:**
- `f` (F)

**Documentation:** _No documentation_

##### `_endpoint_from_view_func(view_func)`

**Line:** 701 | **Returns:** `str`

**Parameters:**
- `view_func` (ft.RouteCallable)

**Documentation:**

> Internal helper that returns the default endpoint for a given
> function.  This always is the function name.

##### `_find_package_path(import_name)`

**Line:** 709 | **Returns:** `str`

**Parameters:**
- `import_name` (str)

**Documentation:**

> Find the path that contains the package or module.

##### `find_package(import_name)`

**Line:** 754 | **Returns:** `tuple[str | None, str]`

**Parameters:**
- `import_name` (str)

**Documentation:**

> Find the prefix that a package is installed under, and the path
> that it would be imported from.
> 
> The prefix is the directory containing the standard directory
> hierarchy (lib, bin, etc.). If the package is not installed to the
> system (:attr:`sys.prefix`) or a virtualenv (``site-packages``),
> ``None`` is returned.
> 
> The path is the entry in :attr:`sys.path` that contains the package
> for import. If the package is not installed, it's assumed that the
> package was imported from the current working directory.

#### Classes

##### Class: `Scaffold`

**Line:** 52

**Documentation:**

> Common behavior shared between :class:`~flask.Flask` and
> :class:`~flask.blueprints.Blueprint`.
> 
> :param import_name: The import name of the module where this object
>     is defined. Usually :attr:`__name__` should be used.
> :param static_folder: Path to a folder of static files to serve.
>     If this is set, a static route will be added.
> :param static_url_path: URL prefix for the static route.
> :param template_folder: Path to a folder containing template files.
>     for rendering. If this is set, a Jinja loader will be added.
> :param root_path: The path that static, template, and resource files
>     are relative to. Typically not set, it is discovered based on
>     the ``import_name``.
> 
> .. versionadded:: 2.0

**Methods:**

- **`__init__(self, import_name, static_folder, static_url_path, template_folder, root_path)`** (Line 75)
  - Returns: `None`
  - _No documentation_

- **`__repr__(self)`** (Line 217)
  - Returns: `str`
  - _No documentation_

- **`_check_setup_finished(self, f_name)`** (Line 220)
  - Returns: `None`
  - _No documentation_

- **`static_folder(self)`** (Line 224)
  - Returns: `str | None`
  - The absolute path to the configured static folder. ``None``

- **`static_folder(self, value)`** (Line 234)
  - Returns: `None`
  - _No documentation_

- **`has_static_folder(self)`** (Line 241)
  - Returns: `bool`
  - ``True`` if :attr:`static_folder` is set.

- **`static_url_path(self)`** (Line 249)
  - Returns: `str | None`
  - The URL prefix that the static route will be accessible from.

- **`static_url_path(self, value)`** (Line 265)
  - Returns: `None`
  - _No documentation_

- **`jinja_loader(self)`** (Line 272)
  - Returns: `BaseLoader | None`
  - The Jinja loader for this object's templates. By default this

- **`_method_route(self, method, rule, options)`** (Line 284)
  - Returns: `t.Callable[[T_route], T_route]`
  - _No documentation_

- **`get(self, rule, **options)`** (Line 296)
  - Returns: `t.Callable[[T_route], T_route]`
  - Shortcut for :meth:`route` with ``methods=["GET"]``.

- **`post(self, rule, **options)`** (Line 304)
  - Returns: `t.Callable[[T_route], T_route]`
  - Shortcut for :meth:`route` with ``methods=["POST"]``.

- **`put(self, rule, **options)`** (Line 312)
  - Returns: `t.Callable[[T_route], T_route]`
  - Shortcut for :meth:`route` with ``methods=["PUT"]``.

- **`delete(self, rule, **options)`** (Line 320)
  - Returns: `t.Callable[[T_route], T_route]`
  - Shortcut for :meth:`route` with ``methods=["DELETE"]``.

- **`patch(self, rule, **options)`** (Line 328)
  - Returns: `t.Callable[[T_route], T_route]`
  - Shortcut for :meth:`route` with ``methods=["PATCH"]``.

- **`route(self, rule, **options)`** (Line 336)
  - Returns: `t.Callable[[T_route], T_route]`
  - Decorate a view function to register it with the given URL

- **`add_url_rule(self, rule, endpoint, view_func, provide_automatic_options, **options)`** (Line 368)
  - Returns: `None`
  - Register a rule for routing incoming requests and building

- **`endpoint(self, endpoint)`** (Line 436)
  - Returns: `t.Callable[[F], F]`
  - Decorate a view function to register it for the given

- **`before_request(self, f)`** (Line 460)
  - Returns: `T_before_request`
  - Register a function to run before each request.

- **`after_request(self, f)`** (Line 487)
  - Returns: `T_after_request`
  - Register a function to run after each request to this object.

- **`teardown_request(self, f)`** (Line 508)
  - Returns: `T_teardown`
  - Register a function to be called when the request context is

- **`context_processor(self, f)`** (Line 542)
  - Returns: `T_template_context_processor`
  - Registers a template context processor function. These functions run before

- **`url_value_preprocessor(self, f)`** (Line 559)
  - Returns: `T_url_value_preprocessor`
  - Register a URL value preprocessor function for all view

- **`url_defaults(self, f)`** (Line 584)
  - Returns: `T_url_defaults`
  - Callback function for URL defaults for all view functions of the

- **`errorhandler(self, code_or_exception)`** (Line 598)
  - Returns: `t.Callable[[T_error_handler], T_error_handler]`
  - Register a function to handle errors by code or exception class.

- **`register_error_handler(self, code_or_exception, f)`** (Line 642)
  - Returns: `None`
  - Alternative error attach function to the :meth:`errorhandler`

- **`_get_exc_class_and_code(exc_class_or_code)`** (Line 657)
  - Returns: `tuple[type[Exception], int | None]`
  - Get the exception class being handled. For HTTP status codes

---

### `C:\tmp\flask\src\flask\sessions.py`

**Language:** python

#### Functions

##### `_lazy_sha1(string)`

**Line:** 276 | **Returns:** `t.Any`

**Parameters:**
- `string` (bytes)

**Documentation:**

> Don't access ``hashlib.sha1`` until runtime. FIPS builds may not include
> SHA-1, in which case the import and use as a default would fail before the
> developer can configure something else.

#### Classes

##### Class: `SessionMixin`

**Line:** 24

**Documentation:**

> Expands a basic dictionary with session attributes.

**Methods:**

- **`permanent(self)`** (Line 28)
  - Returns: `bool`
  - This reflects the ``'_permanent'`` key in the dict.

- **`permanent(self, value)`** (Line 33)
  - Returns: `None`
  - _No documentation_

##### Class: `SecureCookieSession`

**Line:** 57

**Documentation:**

> Base class for sessions based on signed cookies.
> 
> This session backend will set the :attr:`modified` and
> :attr:`accessed` attributes. It cannot reliably track whether a
> session is new (vs. empty), so :attr:`new` remains hard coded to
> ``False``.

**Methods:**

- **`__init__(self, initial)`** (Line 73)
  - Returns: `None`
  - _No documentation_

##### Class: `NullSession`

**Line:** 83

**Documentation:**

> Class used to generate nicer error messages if sessions are not
> available.  Will still allow read-only access to the empty session
> but fail on setting.

**Methods:**

- **`_fail(self, *args, **kwargs)`** (Line 89)
  - Returns: `t.NoReturn`
  - _No documentation_

##### Class: `SessionInterface`

**Line:** 100

**Documentation:**

> The basic interface you have to implement in order to replace the
> default session interface which uses werkzeug's securecookie
> implementation.  The only methods you have to implement are
> :meth:`open_session` and :meth:`save_session`, the others have
> useful defaults which you don't need to change.
> 
> The session object returned by the :meth:`open_session` method has to
> provide a dictionary like interface plus the properties and methods
> from the :class:`SessionMixin`.  We recommend just subclassing a dict
> and adding that mixin::
> 
>     class Session(dict, SessionMixin):
>         pass
> 
> If :meth:`open_session` returns ``None`` Flask will call into
> :meth:`make_null_session` to create a session that acts as replacement
> if the session support cannot work because some requirement is not
> fulfilled.  The default :class:`NullSession` class that is created
> will complain that the secret key was not set.
> 
> To replace the session interface on an application all you have to do
> is to assign :attr:`flask.Flask.session_interface`::
> 
>     app = Flask(__name__)
>     app.session_interface = MySessionInterface()
> 
> Multiple requests with the same session may be sent and handled
> concurrently. When implementing a new session interface, consider
> whether reads or writes to the backing store must be synchronized.
> There is no guarantee on the order in which the session for each
> request is opened or saved, it will occur in the order that requests
> begin and end processing.
> 
> .. versionadded:: 0.8

**Methods:**

- **`make_null_session(self, app)`** (Line 150)
  - Returns: `NullSession`
  - Creates a null session which acts as a replacement object if the

- **`is_null_session(self, obj)`** (Line 162)
  - Returns: `bool`
  - Checks if a given object is a null session.  Null sessions are

- **`get_cookie_name(self, app)`** (Line 171)
  - Returns: `str`
  - The name of the session cookie. Uses``app.config["SESSION_COOKIE_NAME"]``.

- **`get_cookie_domain(self, app)`** (Line 175)
  - Returns: `str | None`
  - The value of the ``Domain`` parameter on the session cookie. If not set,

- **`get_cookie_path(self, app)`** (Line 187)
  - Returns: `str`
  - Returns the path for which the cookie should be valid.  The

- **`get_cookie_httponly(self, app)`** (Line 195)
  - Returns: `bool`
  - Returns True if the session cookie should be httponly.  This

- **`get_cookie_secure(self, app)`** (Line 202)
  - Returns: `bool`
  - Returns True if the cookie should be secure.  This currently

- **`get_cookie_samesite(self, app)`** (Line 208)
  - Returns: `str | None`
  - Return ``'Strict'`` or ``'Lax'`` if the cookie should use the

- **`get_cookie_partitioned(self, app)`** (Line 215)
  - Returns: `bool`
  - Returns True if the cookie should be partitioned. By default, uses

- **`get_expiration_time(self, app, session)`** (Line 223)
  - Returns: `datetime | None`
  - A helper method that returns an expiration date for the session

- **`should_set_cookie(self, app, session)`** (Line 233)
  - Returns: `bool`
  - Used by session backends to determine if a ``Set-Cookie`` header

- **`open_session(self, app, request)`** (Line 249)
  - Returns: `SessionMixin | None`
  - This is called at the beginning of each request, after

- **`save_session(self, app, session, response)`** (Line 263)
  - Returns: `None`
  - This is called at the end of each request, after generating

##### Class: `SecureCookieSessionInterface`

**Line:** 284

**Documentation:**

> The default session interface that stores sessions in signed cookies
> through the :mod:`itsdangerous` module.

**Methods:**

- **`get_signing_serializer(self, app)`** (Line 303)
  - Returns: `URLSafeTimedSerializer | None`
  - _No documentation_

- **`open_session(self, app, request)`** (Line 323)
  - Returns: `SecureCookieSession | None`
  - _No documentation_

- **`save_session(self, app, session, response)`** (Line 337)
  - Returns: `None`
  - _No documentation_

---

### `C:\tmp\flask\src\flask\signals.py`

**Language:** python

_No functions or classes found._

---

### `C:\tmp\flask\src\flask\templating.py`

**Language:** python

#### Functions

##### `_default_template_ctx_processor()`

**Line:** 21 | **Returns:** `dict[str, t.Any]`

**Documentation:**

> Default template context processor.  Replaces the ``request`` and ``g``
> proxies with their concrete objects for faster access.

##### `_render(ctx, template, context)`

**Line:** 123 | **Returns:** `str`

**Parameters:**
- `ctx` (AppContext)
- `template` (Template)
- `context` (dict[str, t.Any])

**Documentation:** _No documentation_

##### `render_template(template_name_or_list, **context)`

**Line:** 136 | **Returns:** `str`

**Parameters:**
- `template_name_or_list` (str | Template | list[str | Template])
- `**context` (t.Any)

**Documentation:**

> Render a template by name with the given context.
> 
> :param template_name_or_list: The name of the template to render. If
>     a list is given, the first name to exist will be rendered.
> :param context: The variables to make available in the template.

##### `render_template_string(source, **context)`

**Line:** 151 | **Returns:** `str`

**Parameters:**
- `source` (str)
- `**context` (t.Any)

**Documentation:**

> Render a template from the given source string with the given
> context.
> 
> :param source: The source code of the template to render.
> :param context: The variables to make available in the template.

##### `_stream(ctx, template, context)`

**Line:** 163 | **Returns:** `t.Iterator[str]`

**Parameters:**
- `ctx` (AppContext)
- `template` (Template)
- `context` (dict[str, t.Any])

**Documentation:** _No documentation_

##### `stream_template(template_name_or_list, **context)`

**Line:** 181 | **Returns:** `t.Iterator[str]`

**Parameters:**
- `template_name_or_list` (str | Template | list[str | Template])
- `**context` (t.Any)

**Documentation:**

> Render a template by name with the given context as a stream.
> This returns an iterator of strings, which can be used as a
> streaming response from a view.
> 
> :param template_name_or_list: The name of the template to render. If
>     a list is given, the first name to exist will be rendered.
> :param context: The variables to make available in the template.
> 
> .. versionadded:: 2.2

##### `stream_template_string(source, **context)`

**Line:** 200 | **Returns:** `t.Iterator[str]`

**Parameters:**
- `source` (str)
- `**context` (t.Any)

**Documentation:**

> Render a template from the given source string with the given
> context as a stream. This returns an iterator of strings, which can
> be used as a streaming response from a view.
> 
> :param source: The source code of the template to render.
> :param context: The variables to make available in the template.
> 
> .. versionadded:: 2.2

#### Classes

##### Class: `Environment`

**Line:** 36

**Documentation:**

> Works like a regular Jinja environment but has some additional
> knowledge of how Flask's blueprint works so that it can prepend the
> name of the blueprint to referenced templates if necessary.

**Methods:**

- **`__init__(self, app, **options)`** (Line 42)
  - Returns: `None`
  - _No documentation_

##### Class: `DispatchingJinjaLoader`

**Line:** 49

**Documentation:**

> A loader that looks for templates in the application and all
> the blueprint folders.

**Methods:**

- **`__init__(self, app)`** (Line 54)
  - Returns: `None`
  - _No documentation_

- **`get_source(self, environment, template)`** (Line 57)
  - Returns: `tuple[str, str | None, t.Callable[[], bool] | None]`
  - _No documentation_

- **`_get_source_explained(self, environment, template)`** (Line 64)
  - Returns: `tuple[str, str | None, t.Callable[[], bool] | None]`
  - _No documentation_

- **`_get_source_fast(self, environment, template)`** (Line 88)
  - Returns: `tuple[str, str | None, t.Callable[[], bool] | None]`
  - _No documentation_

- **`_iter_loaders(self, template)`** (Line 98)
  - Returns: `t.Iterator[tuple[Scaffold, BaseLoader]]`
  - _No documentation_

- **`list_templates(self)`** (Line 108)
  - Returns: `list[str]`
  - _No documentation_

---

### `C:\tmp\flask\src\flask\testing.py`

**Language:** python

#### Functions

##### `_get_werkzeug_version()`

**Line:** 100 | **Returns:** `str`

**Documentation:** _No documentation_

#### Classes

##### Class: `EnvironBuilder`

**Line:** 27

**Documentation:**

> An :class:`~werkzeug.test.EnvironBuilder`, that takes defaults from the
> application.
> 
> :param app: The Flask application to configure the environment from.
> :param path: URL path being requested.
> :param base_url: Base URL where the app is being served, which
>     ``path`` is relative to. If not given, built from
>     :data:`PREFERRED_URL_SCHEME`, ``subdomain``,
>     :data:`SERVER_NAME`, and :data:`APPLICATION_ROOT`.
> :param subdomain: Subdomain name to append to :data:`SERVER_NAME`.
> :param url_scheme: Scheme to use instead of
>     :data:`PREFERRED_URL_SCHEME`.
> :param json: If given, this is serialized as JSON and passed as
>     ``data``. Also defaults ``content_type`` to
>     ``application/json``.
> :param args: other positional arguments passed to
>     :class:`~werkzeug.test.EnvironBuilder`.
> :param kwargs: other keyword arguments passed to
>     :class:`~werkzeug.test.EnvironBuilder`.

**Methods:**

- **`__init__(self, app, path, base_url, subdomain, url_scheme, *args, **kwargs)`** (Line 49)
  - Returns: `None`
  - _No documentation_

- **`json_dumps(self, obj, **kwargs)`** (Line 88)
  - Returns: `str`
  - Serialize ``obj`` to a JSON-formatted string.

##### Class: `FlaskClient`

**Line:** 109

**Documentation:**

> Works like a regular Werkzeug test client, with additional behavior for
> Flask. Can defer the cleanup of the request context until the end of a
> ``with`` block. For general information about how to use this class refer to
> :class:`werkzeug.test.Client`.
> 
> .. versionchanged:: 0.12
>    `app.test_client()` includes preset default environment, which can be
>    set after instantiation of the `app.test_client()` object in
>    `client.environ_base`.
> 
> Basic usage is outlined in the :doc:`/testing` chapter.

**Methods:**

- **`__init__(self, *args, **kwargs)`** (Line 125)
  - Returns: `None`
  - _No documentation_

- **`session_transaction(self, *args, **kwargs)`** (Line 136)
  - Returns: `t.Iterator[SessionMixin]`
  - When used in combination with a ``with`` statement this opens a

- **`_copy_environ(self, other)`** (Line 185)
  - Returns: `WSGIEnvironment`
  - _No documentation_

- **`_request_from_builder_args(self, args, kwargs)`** (Line 193)
  - Returns: `BaseRequest`
  - _No documentation_

- **`open(self, *args, **kwargs)`** (Line 204)
  - Returns: `TestResponse`
  - _No documentation_

- **`__enter__(self)`** (Line 249)
  - Returns: `FlaskClient`
  - _No documentation_

- **`__exit__(self, exc_type, exc_value, tb)`** (Line 255)
  - Returns: `None`
  - _No documentation_

##### Class: `FlaskCliRunner`

**Line:** 265

**Documentation:**

> A :class:`~click.testing.CliRunner` for testing a Flask app's
> CLI commands. Typically created using
> :meth:`~flask.Flask.test_cli_runner`. See :ref:`testing-cli`.

**Methods:**

- **`__init__(self, app, **kwargs)`** (Line 271)
  - Returns: `None`
  - _No documentation_

- **`invoke(self, cli, args, **kwargs)`** (Line 275)
  - Returns: `Result`
  - Invokes a CLI command in an isolated environment. See

---

### `C:\tmp\flask\src\flask\typing.py`

**Language:** python

_No functions or classes found._

---

### `C:\tmp\flask\src\flask\views.py`

**Language:** python

#### Classes

##### Class: `View`

**Line:** 16

**Documentation:**

> Subclass this class and override :meth:`dispatch_request` to
> create a generic class-based view. Call :meth:`as_view` to create a
> view function that creates an instance of the class with the given
> arguments and calls its ``dispatch_request`` method with any URL
> variables.
> 
> See :doc:`views` for a detailed guide.
> 
> .. code-block:: python
> 
>     class Hello(View):
>         init_every_request = False
> 
>         def dispatch_request(self, name):
>             return f"Hello, {name}!"
> 
>     app.add_url_rule(
>         "/hello/<name>", view_func=Hello.as_view("hello")
>     )
> 
> Set :attr:`methods` on the class to change what methods the view
> accepts.
> 
> Set :attr:`decorators` on the class to apply a list of decorators to
> the generated view function. Decorators applied to the class itself
> will not be applied to the generated view function!
> 
> Set :attr:`init_every_request` to ``False`` for efficiency, unless
> you need to store request-global data on ``self``.

**Methods:**

- **`dispatch_request(self)`** (Line 78)
  - Returns: `ft.ResponseReturnValue`
  - The actual view function behavior. Subclasses must override

- **`as_view(cls, name, *class_args, **class_kwargs)`** (Line 86)
  - Returns: `ft.RouteCallable`
  - Convert the class into a view function that can be registered

##### Class: `MethodView`

**Line:** 138

**Documentation:**

> Dispatches request methods to the corresponding instance methods.
> For example, if you implement a ``get`` method, it will be used to
> handle ``GET`` requests.
> 
> This can be useful for defining a REST API.
> 
> :attr:`methods` is automatically set based on the methods defined on
> the class.
> 
> See :doc:`views` for a detailed guide.
> 
> .. code-block:: python
> 
>     class CounterAPI(MethodView):
>         def get(self):
>             return str(session.get("counter", 0))
> 
>         def post(self):
>             session["counter"] = session.get("counter", 0) + 1
>             return redirect(url_for("counter"))
> 
>     app.add_url_rule(
>         "/counter", view_func=CounterAPI.as_view("counter")
>     )

**Methods:**

- **`__init_subclass__(cls, **kwargs)`** (Line 165)
  - Returns: `None`
  - _No documentation_

- **`dispatch_request(self, **kwargs)`** (Line 182)
  - Returns: `ft.ResponseReturnValue`
  - _No documentation_

---

### `C:\tmp\flask\src\flask\wrappers.py`

**Language:** python

#### Classes

##### Class: `Request`

**Line:** 18

**Documentation:**

> The request object used by default in Flask.  Remembers the
> matched endpoint and view arguments.
> 
> It is what ends up as :class:`~flask.request`.  If you want to replace
> the request object used you can subclass this and set
> :attr:`~flask.Flask.request_class` to your subclass.
> 
> The request object is a :class:`~werkzeug.wrappers.Request` subclass and
> provides all of the attributes Werkzeug defines plus a few Flask
> specific ones.

**Methods:**

- **`max_content_length(self)`** (Line 60)
  - Returns: `int | None`
  - The maximum number of bytes that will be read during this request. If

- **`max_content_length(self, value)`** (Line 89)
  - Returns: `None`
  - _No documentation_

- **`max_form_memory_size(self)`** (Line 93)
  - Returns: `int | None`
  - The maximum size in bytes any non-file form field may be in a

- **`max_form_memory_size(self, value)`** (Line 116)
  - Returns: `None`
  - _No documentation_

- **`max_form_parts(self)`** (Line 120)
  - Returns: `int | None`
  - The maximum number of fields that may be present in a

- **`max_form_parts(self, value)`** (Line 143)
  - Returns: `None`
  - _No documentation_

- **`endpoint(self)`** (Line 147)
  - Returns: `str | None`
  - The endpoint that matched the request URL.

- **`blueprint(self)`** (Line 162)
  - Returns: `str | None`
  - The registered name of the current blueprint.

- **`blueprints(self)`** (Line 181)
  - Returns: `list[str]`
  - The registered names of the current blueprint upwards through

- **`_load_form_data(self)`** (Line 197)
  - Returns: `None`
  - _No documentation_

- **`on_json_loading_failed(self, e)`** (Line 212)
  - Returns: `t.Any`
  - _No documentation_

##### Class: `Response`

**Line:** 222

**Documentation:**

> The response object that is used by default in Flask.  Works like the
> response object from Werkzeug but is set to have an HTML mimetype by
> default.  Quite often you don't have to create this object yourself because
> :meth:`~flask.Flask.make_response` will take care of that for you.
> 
> If you want to replace the response object used you can subclass this and
> set :attr:`~flask.Flask.response_class` to your subclass.
> 
> .. versionchanged:: 1.0
>     JSON support is added to the response, like the request. This is useful
>     when testing to get the test client response data as JSON.
> 
> .. versionchanged:: 1.0
> 
>     Added :attr:`max_cookie_size`.

**Methods:**

- **`max_cookie_size(self)`** (Line 247)
  - Returns: `int`
  - Read-only view of the :data:`MAX_COOKIE_SIZE` config key.

---

## 📈 Summary Statistics

- **Total Files Scanned:** 24
- **Total Functions/Methods:** 359
- **Total Classes:** 53
- **Documentation Coverage:** 60.2%
- **Documented Items:** 216
- **Undocumented Items:** 143

---

_Generated by Codebase Scanner_